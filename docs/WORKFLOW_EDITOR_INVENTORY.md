# Workflow editor inventory — what Phase 3 rebuilt

Status: reconciled after the Phase 3 implementation audit. Sections 1–5 are the preserved
pre-Phase-3 baseline; the acceptance checklist below records the current verified state.

Date: 2026-07-20
flow-designer baseline: `5215c7f` (end of Phase 2)
Atlas commit read: `595ef62` at `/Users/seal/Documents/GitHub/atlas-control-plane`

## Why this document exists

At the end of Phase 2, `/workflows/$id` was a read-only Atlas view. The following inventory was
captured before the editor rewrite, so its “Today” and source-line references deliberately
describe that baseline rather than the current files.

Phase 3 did not simply switch the scaffold back on. It rewrote the editor against Atlas, which
cannot express a large part of what the scaffold let a user draw. This document remains the
line-by-line record of that decision; the reconciliation at §6 records what was delivered.

## How to read it

Every capability below was extracted from the source by reading it, and every verdict is
justified against Atlas's own code or specs. Five verdicts are used:

| Verdict                 | Meaning                                                                                    |
| ----------------------- | ------------------------------------------------------------------------------------------ |
| **Gone — Atlas cannot** | Atlas has no way to represent this. It will not come back in any form.                     |
| **Gone — by design**    | Deliberately dropped: fake execution, mock writes, or a bug that should not be reproduced. |
| **Returns, reshaped**   | The capability comes back, but its data, field names, or semantics change to match Atlas.  |
| **Returns as-is**       | Comes back essentially unchanged.                                                          |
| **New work needed**     | Atlas supports it, the scaffold never did. Phase 3 has to add it.                          |

Counts: **214 capabilities** — 31 gone (Atlas cannot), 17 gone
(by design), 113 return reshaped, 23 return as-is, and 30 are
new work Atlas supports but the editor never had.

The single most important number is the last one. **The editor that comes back is not a subset of
the old one — it is mostly a different, larger editor.** The old canvas could draw things Atlas
refuses, and could not express most of what Atlas actually offers.

---

## 1. Gone — Atlas cannot express it (31)

These do not come back. Each row names the Atlas constraint that forbids it.

Four of the nine palette kinds are in here. Atlas's executor accepts exactly four node types —
`worker`, `manager`, `join`, `human_gate` (`atlas/workflows.py:173-174`) — so `trigger`,
`condition`, `loop`, and `fanout` cannot be nodes at all. They are not being removed for tidiness;
a graph containing one is rejected by Atlas on save.

What replaces them:

| Old node    | Atlas representation                                                                                          |
| ----------- | ------------------------------------------------------------------------------------------------------------- |
| `trigger`   | A separate **workflow-trigger resource** (`/api/workflow-triggers`); the graph's entry point is `graph.start` |
| `condition` | An **edge property** (`edge.condition`), authored in an edge inspector                                        |
| `loop`      | A **guarded back-edge** (a cycle plus `policy.max_iterations` or an edge `max_iterations_below` guard)        |
| `fanout`    | **Multiple outgoing edges** — Atlas schedules every edge whose condition matches                              |

| Capability                                                  | Where it lives                       | What it did                                                                                                                                                                                        | Why Atlas cannot express it                                                                                                                                                                                                                                                                                         | Phase 3                                                                                                                                                                                                                                                                                                                                                                                                        |
| ----------------------------------------------------------- | ------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Automatic edge label derivation on connect (condition)      | workflow-editor.tsx:263-268          | For a "condition" source node, the new edge label is config.false_label when sourceHandle==="condition:false", otherwise config.true_label — so an unexpected handle silently gets the TRUE label. | atlas/workflows.py:173 restricts node types to {worker, manager, join, human_gate}; a `condition` node is rejected outright. Conditions exist only as the edge property at workflow-definition.schema.json:142-201.                                                                                                 | The condition node disappears (IMPLEMENTATION_PLAN.md:97). There is no true/false handle pair to derive from. The replacement is an edge inspector where the user picks a condition type from the closed set, and the label rendered on the edge is a formatting of that condition (spec:117 "Display the condition directly on the edge") — derived, never free text.                                         |
| Condition label edits do NOT relabel existing edges         | workflow-editor.tsx:292-307, 944-957 | Editing true_label/false_label patches only node data, so already-drawn edges keep their frozen label and silently diverge from the node's branch rows.                                            | atlas/workflows.py:173 — no `condition` node type exists; there are no true_label/false_label fields anywhere in workflow-definition.schema.json.                                                                                                                                                                   | The bug dies with the node kind. Its lesson must not: in the new model the edge label is a pure render of edge.condition, so drift is structurally impossible. Do not store a denormalized label string on the edge.                                                                                                                                                                                           |
| NODE_KINDS entry: trigger                                   | workflow-scaffold-store.ts:556       | label "Trigger", hint "Webhook, cron, or event"; hint is dead in the UI.                                                                                                                           | atlas/workflows.py:174 - a node with type "trigger" raises "uses unsupported type"; triggers are a separate resource (docs/specs/workflow-trigger.schema.json, routes at atlas/app.py:759-793)                                                                                                                      | Delete the palette entry. Trigger authoring moves to a separate trigger panel backed by /api/workflow-triggers; the graph's entry point is `graph.start` (atlas/workflows.py:206-212), which the canvas should mark on a node badge rather than with a pseudo-node.                                                                                                                                            |
| NODE_KINDS entry: condition                                 | workflow-scaffold-store.ts:558       | label "Condition", hint "Route by expression".                                                                                                                                                     | atlas/workflows.py:174 - "condition" is not in the allowed node-type set; conditions exist only as edge.condition (atlas/workflows.py:223-239, workflow-definition.schema.json $defs/edge.condition)                                                                                                                | Delete the palette entry. Build an edge inspector that authors edge.condition from the closed set: always, artifact_equals, artifact_in, manager_selected, human_selected, max_iterations_below.                                                                                                                                                                                                               |
| NODE_KINDS entry: loop                                      | workflow-scaffold-store.ts:560       | label "Loop", hint "Iterate over items".                                                                                                                                                           | atlas/workflows.py:174 has no "loop" type; iteration is a guarded graph cycle - atlas/workflows.py:255-256 rejects a cycle unless policy.max_iterations or an edge max_iterations_below guard exists                                                                                                                | Delete the palette entry. Loops are drawn as back-edges; the UI must offer the guard (policy.max_iterations, or edge condition max_iterations_below {node,max}) at the moment a cycle is created, and block save otherwise.                                                                                                                                                                                    |
| NODE_KINDS entry: fanout                                    | workflow-scaffold-store.ts:561       | label "Fan-out", hint "Run branches in parallel"; presentation label differs ("Fan out").                                                                                                          | atlas/workflows.py:174 has no "fanout" type; parallelism is emergent - the run loop schedules every outgoing edge whose condition matches (atlas/workflows.py:686-703)                                                                                                                                              | Delete the palette entry. Fan-out is simply multiple outgoing edges from one source handle; pair it with a join node in the UI affordance ("branch and rejoin") rather than a node type.                                                                                                                                                                                                                       |
| DEFAULT_NODE_CONFIG: trigger                                | workflow-editor.tsx:57               | { mode: "webhook", path: "/api/v1/events", time: "09:00", event_workflow: "" } - all four keys always seeded.                                                                                      | atlas/workflows.py:174 rejects a trigger node outright; trigger config shape is fixed by docs/specs/workflow-trigger.schema.json ($defs/schedule config oneOf interval_minutes \| daily_time)                                                                                                                       | Delete. In the trigger panel, seed per-type drafts matching the trigger schema: manual/webhook take an open config object; schedule takes EITHER interval_minutes (>= 1/60) OR daily_time matching ^([01]\\d\|2[0-3]):[0-5]\\d$ - not both, and never a free-text `path` or `time`.                                                                                                                            |
| DEFAULT_NODE_CONFIG: condition                              | workflow-editor.tsx:64-68            | { expr: "payload.status === 'ready'", true_label: "matches", false_label: "otherwise" }; the labels drive branch rows and edge labels.                                                             | workflow-definition.schema.json $defs/condition is a closed oneOf (always \| artifact_equals \| artifact_in \| manager_selected \| human_selected \| max_iterations_below) with additionalProperties:false - no expression type exists; $defs/edge also has additionalProperties:false, so an edge carries no label | Delete both the expression and the branch labels. The edge inspector authors artifact_equals {artifact, path?, value} or artifact_in {artifact, path?, values[]} instead. Any edge caption the UI shows must be derived from the condition and stored nowhere.                                                                                                                                                 |
| DEFAULT_NODE_CONFIG: loop                                   | workflow-editor.tsx:76               | { collection: "payload.items", limit: "100" } - limit is a string.                                                                                                                                 | No loop node exists (atlas/workflows.py:174) and no condition type iterates a collection (workflow-definition.schema.json $defs/condition); the only iteration control is policy.max_iterations or max_iterations_below {node, max}                                                                                 | Delete. Where a user wants "repeat over items", Phase 3 offers a guarded back-edge: an integer `max` (>= 1) on a max_iterations_below condition, or an integer policy.max_iterations (1..100, atlas/workflows.py:263-266). No collection expression is possible.                                                                                                                                               |
| DEFAULT_NODE_CONFIG: fanout                                 | workflow-editor.tsx:77               | { branches: "2" } - a string count that creates no extra handles.                                                                                                                                  | atlas/workflows.py:174 has no fanout type; branch count is implicit in the edge list                                                                                                                                                                                                                                | Delete. Nothing replaces it - the number of parallel paths is just the number of outgoing edges the user draws.                                                                                                                                                                                                                                                                                                |
| Presentation: trigger                                       | workflow-node-presentation.ts:24-30  | label "Trigger", description "Start from an event or schedule", Webhook icon, amber tile/accent.                                                                                                   | atlas/workflows.py:174 - no trigger node type exists                                                                                                                                                                                                                                                                | Remove from NODE_PRESENTATION. Reuse the Webhook icon and amber accent for the separate trigger panel and for a small "start" badge on the graph.start node.                                                                                                                                                                                                                                                   |
| Presentation: condition                                     | workflow-node-presentation.ts:38-44  | label "Condition", GitBranch icon, fuchsia tile/accent.                                                                                                                                            | atlas/workflows.py:174 - conditions are an edge property (atlas/workflows.py:223-239), never a node                                                                                                                                                                                                                 | Remove from NODE_PRESENTATION. Move the GitBranch icon and a token-based accent onto conditional EDGES and the edge inspector header, so the visual vocabulary survives even though the node does not.                                                                                                                                                                                                         |
| Presentation: loop                                          | workflow-node-presentation.ts:52-58  | label "Loop", Repeat icon, fuchsia tile/accent (shared with condition).                                                                                                                            | atlas/workflows.py:255-256 - a loop is a graph cycle plus a guard, not a node type (atlas/workflows.py:174)                                                                                                                                                                                                         | Remove. Reuse the Repeat icon as an edge decoration on a detected back-edge, and use it in the guard-required validation message.                                                                                                                                                                                                                                                                              |
| Presentation: fanout                                        | workflow-node-presentation.ts:59-65  | label "Fan out", Split icon, emerald tile/accent (shared with join).                                                                                                                               | atlas/workflows.py:174 - no fanout type; parallel scheduling is emergent from multiple matching edges (atlas/workflows.py:686-703)                                                                                                                                                                                  | Remove. Keep the emerald identity for join only; if a node has 2+ outgoing edges, the canvas may hint parallelism visually without a node.                                                                                                                                                                                                                                                                     |
| Per-kind branch handles: condition (two)                    | workflow-node.tsx:26-32              | Condition nodes get exactly two source handles, "condition:true" and "condition:false", labelled from config.                                                                                      | atlas/workflows.py:174 has no condition node; conditions live on edges (workflow-definition.schema.json $defs/edge.condition) and there is no true/false pair - artifact_equals/artifact_in either match or the edge is skipped (atlas/workflows.py:692)                                                            | Delete. There is no boolean node with a false branch: the user draws N outgoing edges and gives each its own condition; an "otherwise" path is expressed as a separate edge with a complementary condition or `always`. Say this plainly in the edge inspector so users do not look for a false port.                                                                                                          |
| nodeHint(): trigger                                         | workflow-editor.tsx:105-110          | event -> "After another workflow"; schedule -> "Daily at <time>"; else the path, falling back to "Manual start".                                                                                   | atlas/workflows.py:174 - no trigger node exists to hint                                                                                                                                                                                                                                                             | Delete from the node hint. Re-use the wording in the trigger PANEL rows, corrected to the real trigger types: manual, schedule (interval_minutes or daily_time), webhook, workflow_run_completed, artifact_created, worker_status_changed (docs/specs/workflow-trigger.schema.json $defs/trigger).                                                                                                             |
| nodeHint(): condition                                       | workflow-editor.tsx:115              | Shows the raw config.expr string, falling back to "Evaluate expression".                                                                                                                           | workflow-definition.schema.json $defs/condition is a closed oneOf with no expression form                                                                                                                                                                                                                           | Delete. The equivalent summary belongs on the edge label overlay, rendered from the structured condition (e.g. "status == ready" derived from artifact_equals {artifact, path, value}), never from a user-typed expression.                                                                                                                                                                                    |
| nodeHint(): loop                                            | workflow-editor.tsx:125              | Shows config.collection, else "Iterate items"; `limit` is never surfaced.                                                                                                                          | atlas/workflows.py:174 has no loop node; the only iteration concept is the cycle guard at atlas/workflows.py:255-256                                                                                                                                                                                                | Delete. The guard summary ("max N iterations") belongs on the back-EDGE, and the workflow header should surface policy.max_iterations.                                                                                                                                                                                                                                                                         |
| nodeHint(): fanout                                          | workflow-editor.tsx:126              | Always `<branches> parallel paths`, defaulting to "2".                                                                                                                                             | atlas/workflows.py:174 has no fanout node type                                                                                                                                                                                                                                                                      | Delete. If Phase 3 wants the information, compute "N parallel paths" live from the node's outgoing edge count on any node - it is derived, never configured.                                                                                                                                                                                                                                                   |
| branchMatches(): edge labels ARE respected (condition only) | workflow-simulator.ts:69-76          | Matches a condition branch by sourceHandle or by case-insensitive free-text edge label; decision routing compares labels case-sensitively.                                                         | docs/specs/workflow-definition.schema.json:202-217 - the edge object is additionalProperties:false with only from, to, condition, push_files. There is no label field, and no sourceHandle field, anywhere in the Atlas edge                                                                                        | Free-text edge labels cannot be persisted and can never carry routing meaning. Phase 3 must derive the on-canvas edge caption from the condition object (e.g. 'if status == approved', 'choice: retry', 'always') and keep React Flow sourceHandle purely in local layout state. Any existing label-based semantics must be dropped, not migrated.                                                             |
| Trigger: webhook mode - Endpoint path                       | workflow-editor.tsx:902-909          | Free-text input writing config.path, default '/api/v1/events'.                                                                                                                                     | atlas-control-plane/docs/specs/workflow-trigger.schema.json:10-19 (manual/webhook trigger has only name/type/enabled/config and no path semantics); atlas/app.py:772 fires webhooks at the fixed route POST /api/workflow-triggers/{id}/fire                                                                        | There is no user-chosen webhook path in Atlas. Drop the field. The trigger panel should instead DISPLAY the real fire URL (POST /api/workflow-triggers/{trigger_id}/fire) with a copy button, and note that firing requires the workflows.run permission. Do not invent a path field; \_TRIGGER_CONFIG_KEYS leaves webhook config open, so a stray `path` key would be silently accepted and silently ignored. |
| Condition: Expression field                                 | workflow-editor.tsx:936-942          | Monospace input writing config.expr, seeded with a JavaScript-looking expression.                                                                                                                  | atlas-control-plane/docs/specs/workflow-definition.schema.json:142-201 (condition oneOf is a closed set: always, artifact_equals, artifact_in, manager_selected, human_selected, max_iterations_below - no expression form); atlas/workflows.py:173 forbids a `condition` node entirely                             | Delete the condition node and this field. Atlas has no expression evaluator; the closest equivalents are the EDGE conditions artifact_equals {artifact, path, value} and artifact_in {artifact, path, values[]}, authored in the new edge inspector. Any existing scaffold expr string must fail closed (shown as unconvertible), never be shipped to Atlas.                                                   |
| Condition: Matched path label                               | workflow-editor.tsx:944-950          | Text input writing config.true_label, used by onConnect to label new edges.                                                                                                                        | atlas-control-plane/docs/specs/workflow-definition.schema.json:202-217 (edge: additionalProperties:false, only from/to/condition/push_files - no label)                                                                                                                                                             | Edges carry no persisted label in Atlas. Remove the field; the edge caption must be DERIVED for display from the edge condition (spec §4.3: show e.g. `always` or `fact_check.verdict = approved`). The true/false pair does not exist - a two-way branch is two edges with complementary artifact_equals/artifact_in conditions.                                                                              |
| Condition: Other path label                                 | workflow-editor.tsx:951-957          | Text input writing config.false_label, used for the condition:false handle.                                                                                                                        | atlas-control-plane/docs/specs/workflow-definition.schema.json:202-217 (edge has no label property); atlas/workflows.py:220-239 evaluates edges only by condition type                                                                                                                                              | Same as 'Matched path': delete. There is no implicit else-branch in Atlas - if no outgoing edge condition matches, nothing is scheduled. Phase 3 should warn on a node whose outgoing conditions are not exhaustive rather than fake a 'false' port.                                                                                                                                                           |
| Condition: routing PanelHint                                | workflow-editor.tsx:959-961          | Hint telling users to connect the named output ports of the condition node.                                                                                                                        | atlas/workflows.py:173 (no `condition` node type); docs/BACKEND_INTEGRATION.md:117 (condition is an edge property, 'Unsupported as a node')                                                                                                                                                                         | Dies with the condition node. Replace with guidance in the edge inspector: select an edge, choose its condition type, and the edge caption updates. Named condition output ports disappear from workflow-node.tsx too.                                                                                                                                                                                         |
| Approval: Approvers field                                   | workflow-editor.tsx:1045-1051        | Free-text input writing config.approvers, default 'ops-lead'; drives the node hint.                                                                                                                | atlas-control-plane/docs/specs/workflow-definition.schema.json:110-133 (humanGateNode is additionalProperties:false with only id/type/label/reason/choices - no approver, assignee or role field); atlas/workflows.py:987-998 creates the approval without any assignee                                             | Delete the field. Atlas has no per-gate approver assignment: any identity holding the approvals.decide permission (admin or operator, per docs/BACKEND_INTEGRATION.md:153-154) can decide any pending approval. Say that in a hint instead of pretending to route. If per-gate assignment is genuinely wanted, it is an Atlas backend change and belongs in docs/ATLAS_LIMITATIONS.md, not in the frontend.    |
| Approval: Timeout (seconds)                                 | workflow-editor.tsx:1052-1058        | Text input writing config.timeout_s as a string, default '600'.                                                                                                                                    | atlas-control-plane/docs/specs/workflow-definition.schema.json:110-133 (no timeout/expiry property on humanGateNode); atlas/workflows.py:986-999 parks the run in waiting_for_human with no deadline - the only decisions are approve/choose/reject (workflows.py:625,654,708)                                      | Delete the field. A gate waits indefinitely until someone approves, chooses, or rejects (reject fails the run). The only time bound anywhere near it is workflow-level `policy.max_minutes`, which belongs in the new policy panel. Record the missing per-gate timeout in docs/ATLAS_LIMITATIONS.md.                                                                                                          |
| Fan-out: Parallel paths                                     | workflow-editor.tsx:1100-1106        | Text input writing config.branches as a string; changes nothing on the canvas.                                                                                                                     | atlas/workflows.py:173 (node type must be one of worker/manager/join/human_gate - there is no fanout); docs/BACKEND_INTEGRATION.md:119 ('emergent (multiple matching outgoing edges), not a node')                                                                                                                  | Delete the fanout node and this field. Fan-out is expressed by drawing several outgoing edges from one node - Atlas schedules every edge whose condition matches. Phase 3 should make that discoverable on the canvas (multiple outgoing edges are legal and parallel) rather than via a number field that never did anything.                                                                                 |
| Fan-out: PanelHint                                          | workflow-editor.tsx:1107-1109        | Hint: connect each branch to a step, then use a Join to converge.                                                                                                                                  | atlas/workflows.py:173 (no fanout node type to attach the hint to)                                                                                                                                                                                                                                                  | The node it lives on is gone, but the advice is correct Atlas modelling - move the wording into canvas/edge guidance or the join node's inspector ('multiple outgoing edges run in parallel; converge them with a join').                                                                                                                                                                                      |
| Loop: Collection field                                      | workflow-editor.tsx:1115-1121        | Monospace input writing config.collection, default 'payload.items'.                                                                                                                                | atlas/workflows.py:173 (no loop node type); docs/BACKEND_INTEGRATION.md:118 (loop is a 'guarded graph cycle, not a node'); the closed condition set in docs/specs/workflow-definition.schema.json:142-201 has no iterate-over-collection form                                                                       | Delete. Atlas has no map-over-a-collection primitive at all - there is nothing to migrate this key to. Iteration is only a back-edge in the graph, guarded by policy.max_iterations or an edge `max_iterations_below` condition (atlas/workflows.py:254-256 rejects an unguarded cycle). Log the missing per-item iteration in docs/ATLAS_LIMITATIONS.md.                                                      |
| Loop: Maximum items field                                   | workflow-editor.tsx:1122-1128        | Text input writing config.limit as a string, default '100'.                                                                                                                                        | atlas/workflows.py:1946-1950 (max_iterations_below is an EDGE condition taking {node, max}); docs/specs/workflow-definition.schema.json:245 (policy.max_iterations, 1..100)                                                                                                                                         | Delete as a node field. The nearest real controls are the new edge condition `max_iterations_below {node, max}` and the policy panel's `max_iterations` - both are execution counts of a node, not a collection length, so this is not a rename but a different concept. Do not silently map '100' onto either.                                                                                                |

---

## 2. Gone — by design (17)

Behaviour that will not be reproduced because it was fake, was a bug, or wrote to the mock store.
The largest single item is the entire timer-based simulator: `workflow-simulator.ts` advanced
every node after a fixed 700 ms and could never fail. `IMPLEMENTATION_PLAN.md` (Phase 3) forbids
simulating execution with timers; Run will call `POST /api/workflow-runs` and the canvas will
reflect real Atlas runtime state.

| Capability                                                                                | Where it lives                                                  | What it did                                                                                                                                                                                                                           | Phase 3                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ----------------------------------------------------------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Drag marks the editor dirty only on drag END                                              | workflow-editor.tsx:244-245                                     | handleNodesChange sets dirty when a change is type==="position" and change.dragging !== true, so the flag flips once per completed drag (and also on programmatic moves where dragging is undefined).                                 | Dragging must NOT set the semantic dirty flag any more, otherwise every nudge makes the editor claim unsaved workflow changes and invites a pointless PUT /api/workflows/{id}. Track layout-dirty separately (debounced localStorage write) from graph-dirty (drives Save to Atlas).                                                                                                                                                                                                         |
| Seeded "true"/"else" edge labels come from mock store data, not from the condition config | workflow-scaffold-store.ts:282-283                              | Seed edges e3/e4 hardcode label "true"/"else" while node n3 has no true_label/false_label, so the seeded edges disagree with the node's own default branch rows ("matches"/"otherwise").                                              | Delete the whole mock seed. Any starting content in Phase 3 comes from Atlas (GET /api/workflows) or from the templates the spec mentions at :91, which must themselves be schema-valid definitions.                                                                                                                                                                                                                                                                                         |
| Node deletion — keyboard path does NOT mark dirty and leaves the inspector stranded       | workflow-editor.tsx:241-248                                     | A "remove" NodeChange is forwarded but handleNodesChange only inspects type==="position", so dirty is never set; separately `selected` still holds the deleted id, so the inspector just unmounts with stale state.                   | Straight bug — do not port it. In the rewrite, derive graph-dirty from a comparison/undo stack over the semantic model rather than from an ad-hoc scan of NodeChange types, and drive selection from onSelectionChange so a removed node clears it. Losing a delete silently is worse against Atlas than against a mock, because Save is now a real PUT.                                                                                                                                     |
| Edge run-animation is computed but never applied                                          | workflow-editor.tsx:150, 222, 457-464                           | toFlow computes animated from runStates, but is called once in a useMemo over [workflow] with no runStates, so animated is always false; the run-state effect refreshes node data only and never touches edges.                       | The dead code goes. Real edge highlighting is possible but must be derived from Atlas run state (runtime nodes + per-job SSE, Phase 4), not from a local timer, and it belongs in a run/observability view rather than the authoring canvas. Do not port the animated field into the Phase 3 editor at all.                                                                                                                                                                                  |
| Mock store reads: mutation actions                                                        | workflow-editor.tsx:234-236                                     | Pulls updateWorkflow (stamps updated_at:"just now"), addRun (mints run_000NN from 215 + runs.length, seeds node_states, returns "" for an unknown workflow id - unchecked by the editor) and updateRun from the mock Zustand store.   | Delete workflow-scaffold-store.ts entirely along with these actions. updateWorkflow -> PUT /api/workflows/{id}; addRun -> POST /api/workflow-runs (the id comes back from Atlas, and the "" -> unchecked-empty-id bug disappears because a bad id is a 4xx the mutation must surface); updateRun has no equivalent and must not be recreated - run state is read from Atlas. Keep only the presentational modules (workflow-node.tsx, workflow-node-presentation.ts), trimmed to four kinds. |
| defaultedConfig() default-merge and legacy trigger-mode inference                         | workflow-editor.tsx:95-101                                      | Merges DEFAULT_NODE_CONFIG under the stored config, and for triggers infers mode from a `cron` key or the substring "manual" in the label.                                                                                            | Delete the label-substring inference outright - it is heuristic guessing over a mock shape. Keep a narrow, typed normalizer for Atlas's documented import defaults only (workflow-visual-builder-spec-en.md section 14: add condition `always`, join `mode: all`, manager `schema: manager_decision_v1`). Anything else unknown must fail closed per the plan, not be silently defaulted.                                                                                                    |
| Local mutable mirrors currentStates / currentLogs                                         | workflow-editor.tsx:415-451                                     | Closure-level mutable copies of run states and logs kept in sync with React state because timer callbacks would read stale state.                                                                                                     | Delete entirely. Run state lives in the react-query cache keyed by run id; there are no timer callbacks so there is no staleness problem to work around. Any 'latest known' bookkeeping is only the last consumed event seq for incremental fetching.                                                                                                                                                                                                                                        |
| createWorkflowSimulator: graph indexing                                                   | workflow-simulator.ts:37-53                                     | Builds nodeById/outgoing/incoming adjacency plus started/active/joinArrivals/pending/finished/stopped mutable execution state.                                                                                                        | The whole execution-state half is deleted. Keep a small pure adjacency helper only for pre-save client validation (quorum <= distinct incoming, cycle+guard, orphan detection) and for auto-layout - never for deciding what runs.                                                                                                                                                                                                                                                           |
| Fixed 700 ms per-node duration                                                            | workflow-simulator.ts:194-197                                   | Every non-gate node takes exactly 700 ms via one uncancellable setTimeout; cancel() only sets a stopped flag.                                                                                                                         | Delete the timer. Elapsed time is rendered from started_at (and finished_at when terminal) on the Atlas runtime node; the only permitted timer is the poll/backoff interval of the data layer, and it must be cleared on unmount.                                                                                                                                                                                                                                                            |
| Success is unconditional — no failure path from nodes                                     | workflow-simulator.ts:135-156, workflow-simulator.ts:195        | completeNode always writes success; the only failure path is a human rejecting an approval; condition nodes always pass so the false branch is unreachable.                                                                           | Failure becomes first-class: render the runtime node error string, the failed/cancelled/interrupted states, and the run-level error from GET /api/workflow-runs/{id}. Condition outcomes are no longer invented - they come from edge_taken vs condition_skipped events (atlas/workflows.py:692, 878).                                                                                                                                                                                       |
| completeNode(): per-kind completion log                                                   | workflow-simulator.ts:135-156                                   | Writes hand-authored success lines per node kind ('Condition matched.', 'Manager selected the first connected path.', '{label} completed.').                                                                                          | The UI stops authoring narrative text. It renders Atlas event rows (event_type + node_key + payload + created_at) and maps event_type to a localized label in a single presentation module. Manager selection shows the actual decision payload, not a placeholder sentence.                                                                                                                                                                                                                 |
| route(): edge traversal and label matching                                                | workflow-simulator.ts:101-133                                   | Client-side routing: fan-out to all edges by default; decision filters by sourceHandle/label; condition filters by branch with a first-edge fallback; manager always takes the first connected edge; loop/fanout config ignored.      | Routing leaves the client completely. What remains is (a) an edge inspector that authors the condition object and (b) run visualisation that highlights edges from edge_taken events and dims condition_skipped ones. The manager 'first edge' fallback and the condition first-edge fallback must not be reimplemented - they would silently contradict Atlas.                                                                                                                              |
| Log line production (onLog)                                                               | workflow-editor.tsx:434-447                                     | Client appends a log record per simulator callback, resolving the node label to uppercase (or passing the pseudo-id SYSTEM through) and writing the entire log array back to the store on every line.                                 | Deleted. The read path fetches events (seq-ordered ascending, atlas/db.py:1349-1355) and appends only what is new. Node labels for display are looked up from the graph by node_key; run-level events have node_key = null and render as the run scope, replacing the fake SYSTEM pseudo-node.                                                                                                                                                                                               |
| Complete log vocabulary                                                                   | workflow-simulator.ts:65-235                                    | Seven hand-written message shapes invented by the client ('Running X…', 'Condition matched.', 'Workflow completed successfully.', etc.).                                                                                              | Build one presentation module mapping every Atlas event_type to a label, icon and severity, with an explicit fallback that renders unknown event types verbatim rather than dropping them. None of the seven scaffold sentences survives; in particular 'Workflow completed successfully.' must not be shown for a run_finished whose payload state is failed or cancelled.                                                                                                                  |
| Log list is UNBOUNDED and not virtualized                                                 | workflow-editor.tsx:662-681                                     | Renders every log line into one DOM tree with no cap, no windowing, full-array copy per line, no auto-scroll - violating the CLAUDE.md live-log rule.                                                                                 | Must be fixed, not ported: bounded ring buffer (fixed max retained events) plus a virtualized list, append-only by seq with a 'jump to latest' control and pinned-to-bottom auto-scroll. Fetch incrementally rather than re-reading the whole history each tick. This is a hard CLAUDE.md requirement, so it is Phase 3/4 acceptance criteria, not a nicety.                                                                                                                                 |
| addRun(workflowId) store write                                                            | workflow-scaffold-store.ts:513-540                              | Mints a client-side id run_00215+n (collides after deletions), fabricates started_at as a locale time string, duration_ms 0, triggered_by 'manual', guesses first-node-running, and seeds a log line that run() immediately discards. | Delete. The client never mints run ids or timestamps. triggered_by/provenance comes from the run input \_meta envelope Atlas validates and audits (atlas/workflows.py:96-140, 143-155). started_at/finished_at are ISO-8601 UTC from Atlas (now_iso), so the UI must format, never generate, them.                                                                                                                                                                                           |
| updateRun writes during a run                                                             | workflow-editor.tsx:422-451, workflow-scaffold-store.ts:509-510 | Client shallow-merges run patches on every state change and every log line, flips run.state between paused and running from node states, never updates duration_ms, and never marks a run cancelled.                                  | Delete every write. The run record is read-only to the client. Note the semantic correction: Atlas 'paused' means an operator called POST /api/workflow-runs/{id}/pause - it is NOT what a human gate does (that is waiting_for_human). The scaffold's node-state-drives-run-state mapping must not be reproduced. Duration is finished_at - started_at, computed for display only.                                                                                                          |

---

## 3. Returns, reshaped (113)

The bulk of the editor. These come back, but the data underneath changes — field names become
Atlas's names, values become Atlas ids, and validation becomes real.

#### Other (113)

| Capability                                                            | Where it lives                                                                                            | What it did                                                                                                                                                                                                                                                                                                         | What Phase 3 changes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| React Flow canvas host (Background + Controls + MiniMap)              | workflow-editor.tsx:577-605                                                                               | ReactFlowProvider + ReactFlow with Background (dots, gap 22), Controls (showInteractive false), MiniMap (hardcoded #15cfe2 node colour), hideAttribution; the dot grid is painted twice (Background plus a .workflow-canvas radial-gradient).                                                                       | Keep the host as-is structurally, but it must gain a Start badge on the start node (spec :125) which the current canvas has no concept of. Fix the two violations of this repo's own rules while rebuilding: the duplicated grid painting (drop one) and the hardcoded MiniMap hex — CLAUDE.md forbids colour literals, so nodeColor must come from a design token.                                                                                                                                                                                                                                                                                                                            |
| fitView on mount only                                                 | workflow-editor.tsx:588-589                                                                               | fitView with padding 0.24 runs once on first render; no useReactFlow handle, no re-fit after addNode/delete/workflow change, so a newly added node can land off-viewport.                                                                                                                                           | Keep fitView, but Phase 3 must add a useReactFlow handle anyway (needed for screenToFlowPosition on add, and to focus a node/edge when a validation error is clicked — spec:96 "Selecting an error must focus the affected node, edge, or field"). Re-fit or pan-to-node after add/auto-layout falls out of that same handle.                                                                                                                                                                                                                                                                                                                                                                  |
| Node drag and position capture                                        | workflow-editor.tsx:241-248, 371-383                                                                      | Positions live in React Flow's node array via useNodesState/onNodesChange and are read back at save time from node.position and written to the mock store as WorkflowNode.x/.y.                                                                                                                                     | Dragging returns, but the save path must be split in two: semantic graph -> Atlas, positions -> localStorage keyed by workflow id + graph version, with auto-layout fallback (IMPLEMENTATION_PLAN.md:106). Sending x/y inside a node would be rejected by the editor-profile schema; note Atlas's own runtime validator (workflows.py:149-233) does not police unknown node keys, so the fail-closed serializer has to strip them itself.                                                                                                                                                                                                                                                      |
| Edge creation via onConnect                                           | workflow-editor.tsx:258-287                                                                               | Spreads the raw Connection into addEdge with an inline stroke style and an optional derived label; sets no id/type/animated/markerEnd, so edges render as unmarked bezier curves with xyflow-generated ids.                                                                                                         | onConnect must create an edge carrying a condition object, defaulting to {type:"always"} — except when the source is a manager (workflows.py:224-225 forces manager_selected with target === edge.to) or a human_gate with choices (workflows.py:238-239 forces human_selected), where the correct condition must be synthesized at connect time or the save will 400. Also add markerEnd: spec:116 requires a directional arrow on every edge. Edge ids stay UI-local; Atlas identifies an edge by (from,to).                                                                                                                                                                                 |
| Automatic edge label derivation on connect (decision)                 | workflow-editor.tsx:269-273                                                                               | For a "decision" source with sourceHandle "choice:<id>", the label is the matching ChoiceOption.label; no match leaves the edge unlabelled.                                                                                                                                                                         | This survives as the human_gate case, but the derived artifact must be the CONDITION, not just a label: connecting from choice handle c must emit condition {type:"human_selected", choice:c}. The label shown is then rendered from that condition. Keep the per-choice source handles — this is the one branch-port concept Atlas actually models.                                                                                                                                                                                                                                                                                                                                           |
| Decision choice edits DO relabel and prune existing edges             | workflow-editor.tsx:309-341                                                                               | updateDecisionChoices rewrites config.choices, deletes edges whose choice handle no longer exists, and rewrites surviving choice-edge labels; the only place edges are mutated from config.                                                                                                                         | Keep this cascade and generalize it — it is exactly the refactor-references behaviour the spec demands. When a gate choice is deleted, delete or invalidate the edge whose condition.choice referenced it, otherwise Atlas rejects the whole save. Extend the same discipline to node id rename/delete (edges, manager_selected.target, max_iterations_below.node, graph.start) and to human_gate choice reordering (spec:636 — do not sort choices, order is UX).                                                                                                                                                                                                                             |
| Target handles: exactly one, unnamed, suppressed on trigger           | workflow-node.tsx:76-82                                                                                   | One unnamed target Handle on the left, rendered only when kind !== "trigger", so every incoming edge has targetHandle === null.                                                                                                                                                                                     | A single unnamed target handle is correct for all four Atlas kinds and should stay. The trigger exception disappears with the trigger node (atlas/workflows.py:173 rejects it). What replaces it: the start node needs a visible Start badge (spec:125-127) and a "Set as start" action, because graph.start is required (workflows.py:212-216) and no UI affordance exists for it today.                                                                                                                                                                                                                                                                                                      |
| Source handles: kind-specific, named only for condition/decision      | workflow-node.tsx:26-40, 98-130                                                                           | branchOptions gives condition two handles and decision one per choice; every other kind gets a single unnamed source handle, so fanout ignores config.branches and join/loop have no multi-port modelling.                                                                                                          | Reduce to two cases: human_gate with choices renders one named source handle per choice (feeding human_selected); worker/manager/join render one unnamed source handle. Manager multi-way routing uses that single handle with N outgoing manager_selected edges — do not invent manager ports. Fan-out is just multiple edges from the one handle.                                                                                                                                                                                                                                                                                                                                            |
| Branch handle vertical placement is hardcoded pixels                  | workflow-node.tsx:107-113                                                                                 | Each branch handle is absolutely positioned with inline top: `${70 + index * 20}px`, a magic offset tuned to the current header layout; more choices or a header change desynchronises dot from label row.                                                                                                          | Rebuild the handle as a child of its choice row using CSS positioning (position:relative row, handle at right, translate) so it tracks the row automatically. Non-optional now: human_gate choices are unbounded (schema :119-131 has no maxItems), and spec:134 requires 44x44 CSS px touch targets — the current 12px dot fails that.                                                                                                                                                                                                                                                                                                                                                        |
| Edge deletion                                                         | workflow-editor.tsx:250-256                                                                               | No delete affordance and no onEdgesDelete; deletion only via React Flow's default Backspace on a selected edge, which arrives as a "remove" EdgeChange and sets dirty.                                                                                                                                              | Add an explicit delete control in the new edge inspector (and/or context menu) alongside the key path, route it through onEdgesDelete, and include it in the undo stack. Deleting an edge can invalidate a join: workflows.py:252-254 rejects a quorum greater than the distinct incoming upstream count, so re-run validation after every edge removal.                                                                                                                                                                                                                                                                                                                                       |
| Node deletion — button path                                           | workflow-editor.tsx:361-369, 745-752, 1150-1156                                                           | removeSelected drops the node, filters every edge touching it, clears selection, sets dirty; wired to the inspector header trash icon and the footer Delete button. No context menu, no on-canvas affordance.                                                                                                       | Keep the cascade edge cleanup (it is what keeps \_validate_edge happy) and add three things: the start-node guard from spec:127-129, cleanup of dangling references in surviving edge conditions (manager_selected.target, max_iterations_below.node), and deletion of the node's localStorage layout entry (spec:617).                                                                                                                                                                                                                                                                                                                                                                        |
| Selection is tracked twice, in two disconnected systems               | workflow-editor.tsx:225, 585-586, 289-290                                                                 | React Flow's own node.selected flag (used for the ring at workflow-node.tsx:58-59) coexists with a separate useState `selected` written only by onNodeClick; no onSelectionChange, so multi-select highlights several nodes while the inspector shows the last-clicked one.                                         | Collapse to one source of truth via onSelectionChange, and widen the selection type to a discriminated union — the inspector must now serve node OR edge OR workflow OR policy OR trigger (spec:94). Selection also becomes the target of error focus (spec:96).                                                                                                                                                                                                                                                                                                                                                                                                                               |
| What selecting a node does                                            | workflow-editor.tsx:688-700, workflow-node.tsx:47-60                                                      | Mounts a 340px NodeInspector aside that takes layout width (shrinking the canvas) with kind-specific forms, and rings the node — but only when runState is absent, since runTone checks runState before falling through to selected.                                                                                | Keep the aside; replace its contents with the four Atlas kinds and route edge/policy/trigger selections into it too. Fix the ring precedence so selection remains visible during a run (during live runs users click nodes precisely to inspect them). Node bodies must additionally surface id, type and validation state, and worker/manager routing + outputs[0].                                                                                                                                                                                                                                                                                                                           |
| Dirty-state tracking — the complete set of triggers                   | workflow-editor.tsx:230, 497-501                                                                          | setDirty(true) from seven call sites (drag-end, edge remove, connect, config edit, decision choices, addNode, removeSelected); cleared only in save(). Not tracked: keyboard node delete, selection, pan/zoom. Renders only an "Unsaved changes" pill hidden below sm; no beforeunload guard and no router blocker. | Split into layout-dirty (local, autosaved) and graph-dirty (Atlas). Graph-dirty must be complete — the keyboard delete hole must close — and should back an undo/redo stack. Add a router blocker/beforeunload now that discarded edits mean a lost PUT, and capture the loaded workflow version so a stale save can be detected client-side (no ETag exists: db.py:1102 update_workflow_definition has no optimistic-concurrency guard).                                                                                                                                                                                                                                                      |
| Save — what actually gets persisted                                   | workflow-editor.tsx:371-394, 396-398                                                                      | save() writes {id,kind,label,x,y,config} nodes and {id,source,target,label,sourceHandle,targetHandle} edges into the mock Zustand store, dropping node hint and edge style/animated; called by the header Save, the inspector footer, and implicitly by run().                                                      | Rewrite save() as a serializer that emits {name, description, graph:{start,nodes,edges}, policy} — every UI-only field (x, y, hint, style, handles, edge ids, kind aliases) stripped, defaults written explicitly (join.mode, manager.schema, edge.condition — spec:634), and any unknown node/condition type failing closed instead of being sent. Positions go to localStorage in the same commit. Note save() is currently implicit in run(): Phase 3 must decide save-then-POST /api/workflow-runs explicitly, and surface Atlas ValueError messages onto the offending node/edge/field (plan exit criteria, IMPLEMENTATION_PLAN.md:113).                                                  |
| Node id generation collides silently                                  | workflow-editor.tsx:344                                                                                   | addNode builds ids as `n_${Math.random().toString(36).slice(2,7)}` — 5 base-36 chars with no uniqueness check against existing nodes.                                                                                                                                                                               | Collisions become a server-side 400 instead of a silent mock overwrite. Generate ids checked against the current node set (or use crypto.randomUUID / a slugified user-facing id), validate uniqueness locally before save, and make node id user-editable with reference refactoring, since ids are visible in Atlas conditions (manager_selected.target, max_iterations_below.node) and in the run view.                                                                                                                                                                                                                                                                                     |
| Edge styling is applied three redundant ways                          | workflow-editor.tsx:151, 279, 591-593                                                                     | The same stroke object is set inline in toFlow, inline again in onConnect, and as defaultEdgeOptions, on top of a .react-flow\_\_edge-path rule in src/styles.css:213-216 that the inline styles override.                                                                                                          | Collapse to one source: defaultEdgeOptions plus CSS, with markerEnd added there (spec:116 requires arrows). Styling must then carry semantics it does not today — condition type distinguishable at a glance, back-edges routed as visible return paths (spec:118), and a non-colour-only selected/error treatment (spec:119).                                                                                                                                                                                                                                                                                                                                                                 |
| No edge reconnection                                                  | workflow-editor.tsx:579-594                                                                               | onReconnect/onEdgeUpdate and reconnectRadius are absent, so an edge endpoint cannot be dragged elsewhere — rerouting means delete and redraw.                                                                                                                                                                       | Optional to add, but if added, onReconnect must rewrite the coupled condition fields (manager_selected.target follows the new `to`; human_selected must be revalidated against the new source gate's choices) and then re-run validation — a naive endpoint swap produces a graph Atlas rejects. A menu-based "Connect to..." alternative is required either way.                                                                                                                                                                                                                                                                                                                              |
| Canvas hint overlay                                                   | workflow-editor.tsx:607-610, 571-573                                                                      | A pointer-events-none pill at top-left reading "Canvas / drag to arrange / connect ports to link", shown only at xl and above, mirrored by a static hint card in the left palette.                                                                                                                                  | Low value as-is; the canvas overlay slot should be reused for something load-bearing — validation status / error count that stays discoverable without hover, and a graph-level indicator when no start node is set. Keep the palette hint but retarget it at the four Atlas node kinds.                                                                                                                                                                                                                                                                                                                                                                                                       |
| Workflow name + status line (header identity block)                   | workflow-editor.tsx:477-496                                                                               | Zap tile + read-only <h1>{workflow.name}, then a status row driven by workflow.trigger_enabled plus a live "{nodes.length} steps" count.                                                                                                                                                                            | Name comes from GET /api/workflows/{id} (app.py:589-591) and is now editable via PUT, so add a rename control. Replace the single boolean status with a summary derived from the workflow's trigger collection (0..N triggers, each with its own `enabled`) - e.g. "2 of 3 triggers enabled". Keep "{nodes.length} steps" as pure UI derived from graph.nodes.length.                                                                                                                                                                                                                                                                                                                          |
| "Unsaved changes" pill                                                | workflow-editor.tsx:497-501,241-256,230                                                                   | Amber "Unsaved changes" pill gated on a single boolean `dirty`, set on drag-end position changes, edge remove, connect, config/label edits, add/remove node - but NOT on React Flow's own keyboard node deletion.                                                                                                   | Split into two flags: semantic-dirty (nodes/edges/conditions/policy/name -> requires a PUT) and layout-dirty (positions/viewport -> localStorage only, must never light the pill or block navigation). Fix the bug at line 244 by treating NodeChange type "remove" as dirty too. The pill is also the only lost-update defense the user gets, because Atlas exposes no ETag/If-Match (docs/IMPLEMENTATION_PLAN.md Phase 3, "Atlas has no ETag/If-Match") - keep it visible at all breakpoints, not sm:inline.                                                                                                                                                                                 |
| Enable/Disable trigger button                                         | workflow-editor.tsx:504-513                                                                               | lg-only text button toggling workflow.trigger_enabled directly on the mock store, outside Save, without setting dirty; its own label cannot update because the component reads the prop, not the store.                                                                                                             | A workflow has 0..N triggers, so one global toggle no longer maps. Either move the control into a trigger panel with a per-trigger switch calling PUT /api/workflow-triggers/{id} {enabled}, or keep a header shortcut only when exactly one trigger exists. Must invalidate the trigger query on success, surface 4xx from Atlas, and gate on workflows.manage for UX only (Atlas is the authority). Note PUT re-runs \_prepare_workflow_trigger, so an enable can legitimately fail (e.g. unknown workflow_definition_id) - needs an error path.                                                                                                                                             |
| Save button                                                           | workflow-editor.tsx:371-394,522                                                                           | Calls save(), which writes {nodes: [{id,kind,label,x,y,config}], edges: [{id,source,target,label,sourceHandle,targetHandle}]} into the in-memory Zustand store, stamps updated_at, clears dirty. No network, no toast, no error path, no name/description/status write; lost on reload.                             | Rewrite save() as a server function calling PUT /api/workflows/{id}. Serialization changes shape completely: `source/target` -> `from/to`; every edge must carry an explicit `condition` object; `sourceHandle`/`targetHandle` and the edge `label` are UI-only and must be dropped (edge additionalProperties:false will 400 otherwise); `kind` -> `type` restricted to worker\|manager\|join\|human_gate; `x`/`y` go to localStorage keyed by workflow id (spec §13, lines 600-618); `graph.start` must be emitted. Needs pending/success/error UI, error mapping to the offending node/edge, query invalidation, and a client-side lost-update guard because Atlas has no ETag.             |
| Run button                                                            | workflow-editor.tsx:396-455,529                                                                           | Cancels the in-flight simulator, calls save(), mints a fake run id via addRun, seeds every node "queued", force-opens the drawer, and starts createWorkflowSimulator with timer-driven node states, logs and gates.                                                                                                 | Run becomes: (1) validate, (2) PUT the graph if semantically dirty, (3) POST /api/workflow-runs and take the returned run id. The entire timer path in workflow-simulator.ts (245 lines) is deleted - node states now come from GET /api/workflow-runs/{id} (app.py:662-675, returns {run, nodes, edges, approvals}) combined with per-job SSE (BACKEND_INTEGRATION.md:139: there is no unified live run stream). Make the implicit save explicit in the UI, and keep Run disabled while validation fails.                                                                                                                                                                                     |
| Left palette aside                                                    | workflow-editor.tsx:538-570                                                                               | 256px lg-only column headed "Add a step", rendering one button for each of the nine NODE_KINDS (trigger, worker, condition, decision, loop, fanout, join, approval, manager) using NODE_PRESENTATION label/description.                                                                                             | Palette drops from nine entries to four: worker, manager, join, human_gate (displayed as "Approval"). `trigger`, `condition`, `loop`, `fanout` and `decision` must NOT become palette nodes - trigger moves to a separate trigger panel, condition to the edge inspector, loop to a guarded back-edge, fanout to multiple outgoing edges, and `decision` collapses into human_gate.choices. Per docs/IMPLEMENTATION_PLAN.md Phase 3 no round-trip machinery may be added to fake them. Also give the palette a below-lg affordance, since today it simply vanishes.                                                                                                                            |
| Palette hint card (bottom of aside)                                   | workflow-editor.tsx:571-573                                                                               | Static bordered card: "Select a step to add it, then drag it into place and connect its ports on the canvas."                                                                                                                                                                                                       | Keep the card, rewrite the copy. It currently promises drag-and-drop that does not exist (addNode is click-only) and says nothing about where conditions and triggers now live. New copy should point at the edge inspector for conditions and the trigger panel for scheduling.                                                                                                                                                                                                                                                                                                                                                                                                               |
| Palette click → addNode                                               | workflow-editor.tsx:343-359,56-85                                                                         | Creates a node with a random id n_xxxxx at a random flow-coordinate position, label from NODE_KINDS, hint from NODE_PRESENTATION, config shallow-copied from DEFAULT_NODE_CONFIG; selects it and sets dirty.                                                                                                        | Ids stay client-generated but must be stable, unique and human-meaningful (they appear verbatim in Atlas error strings and in edge condition targets), and must be re-checked for collision on import. DEFAULT_NODE_CONFIG becomes Atlas-shaped per-type defaults: worker/manager get prompt + optional worker_id/workspace_id, manager always emits schema:"manager_decision_v1", join always emits mode (default "all"), human_gate gets label/reason/choices. Replace the random position with auto-layout placement in the current viewport, written to the local layout store, not to Atlas.                                                                                              |
| Bottom log drawer — mount condition and heights                       | workflow-editor.tsx:612-615                                                                               | <section> mounts only when runId is non-null; height is h-11 collapsed, h-52 expanded, h-72 expanded-with-pendingGate, with a 200ms transition.                                                                                                                                                                     | Heights and the transition are pure UI and survive. The mount condition changes: the drawer should also open for an already-running Atlas run when the editor loads (e.g. via ?run=, or the workflow's latest non-terminal run), not only for a run started in this tab. The pendingGate height branch becomes an approvals-present branch driven by GET /api/approvals?run_id=.                                                                                                                                                                                                                                                                                                               |
| Log drawer title and "Open full view"                                 | workflow-editor.tsx:627-637                                                                               | Pulsing dot + "Live run {runId}" plus a sm-only "Open full view" button navigating to /runs/$id, unguarded by dirty.                                                                                                                                                                                                | Title text survives but the id is now an Atlas run id, and the pulsing dot must reflect the real run.state (running/paused/succeeded/failed/cancelled) rather than pulsing forever. The /runs/$id route already exists (src/routes/_app/runs.$id.tsx), so the link works; it must go through the same unsaved-changes blocker as the back link.                                                                                                                                                                                                                                                                                                                                                |
| Log drawer "Clear" button                                             | workflow-editor.tsx:639-651                                                                               | Nulls runId (unmounting the drawer), empties runStates, clears pendingGate, cancels the simulator - but leaves `logs` populated and leaves the run permanently "running" in the mock store.                                                                                                                         | Split into two controls with honest labels: "Hide" (detach the drawer view, purely local, and it must also reset the logs buffer that today leaks) and a real "Cancel run" calling POST /api/workflow-runs/{id}/cancel. The stale-"running" bug disappears by construction because Atlas owns run state - but only if Phase 3 refetches instead of caching a local copy. Phase 3 should also add pause/resume, which Atlas supports and this editor never had.                                                                                                                                                                                                                                 |
| Log drawer body (log list + gate card)                                | workflow-editor.tsx:653-683,163-219                                                                       | Scrollable mono list; renders a RunGate approval (Reject/Approve) or choice card from pendingGate, then every log line as {ts} [{NODE}] {text} with level colouring - unbounded, keyed by index, not virtualised, no auto-scroll.                                                                                   | The gate card returns but is now backed by real approvals: poll/refetch GET /api/approvals?run_id={id}&state=pending and post approve/reject/choose. Approve/Reject maps to a gate with no choices; the choice buttons map to gate.choices and must send the choice id Atlas declared (a blank label must not be papered over with "Untitled choice" - reject it at authoring time instead, since workflows.py:190-191 requires a non-empty label). The log list must be rebuilt from per-job SSE events plus run events (app.py:709-715), and per CLAUDE.md must be bounded and virtualized/incrementally rendered, keyed by SSE seq (not array index), with dedupe and auto-scroll-with-pin. |
| How the editor receives its workflow, and mount behaviour             | workflow-editor.tsx:221-224,130-154,95-101,457-464                                                        | Takes a single `workflow` prop, seeds useNodesState/useEdgesState from toFlow(workflow) once (so later prop changes never re-seed the canvas), derives display hints, merges DEFAULT_NODE_CONFIG and back-infers legacy trigger modes; no mount fetch or loader.                                                    | Feed the editor from a route loader / server function on GET /api/workflows/{id}, keeping the \*.functions.ts boundary. toFlow becomes an Atlas adapter in src/lib/ implementing the spec's import pipeline, merging graph geometry with the localStorage layout (spec §13). Delete the legacy trigger back-inference at lines 95-101 - trigger nodes cannot exist in an Atlas graph. Any unknown node type or condition type must fail closed and refuse to load rather than being silently dropped (spec:618 "must not silently retain unknown semantic fields"). Fix the prop/state desync by keying the editor on workflow.id + version so a refetch re-seeds instead of drifting.         |
| Mock store reads: workers list                                        | workflow-editor.tsx:237,691,787-800                                                                       | Subscribes to five hard-coded worker fixtures and renders them in the worker node's "Which worker?" select, prepending an "Auto — choose a healthy worker" option (value "auto"), defaulting to the literal "wrk_01", and labelling but not disabling offline workers.                                              | Populate from the Atlas workers endpoint. The config key changes from `worker` to `worker_id`, and "auto" must be encoded as _omitting_ worker_id (a literal "auto" would be rejected by workflows.py:327-328), optionally paired with `role`/`tags` so Atlas resolves the worker. The hard-coded "wrk_01" default must go. Add workspace_id/workspace_key with the ownership constraint enforced client-side for UX, and filter the option list against policy.allowed_worker_ids so the user cannot author a graph the save will reject.                                                                                                                                                     |
| Mock store reads: workflows list                                      | workflow-editor.tsx:238,692,910-927                                                                       | Subscribes to four fixture workflows, excludes the current one, and offers them in the trigger node's "Start after workflow" select for trigger mode `event`.                                                                                                                                                       | The capability survives, the host does not. Move the picker into the trigger panel as a `workflow_run_completed` trigger with config.source_workflow_definition_id, sourced from GET /api/workflows. Add the `state` filter the schema allows (the editor never had it). Keep excluding the current workflow - Atlas would silently skip a direct self-trigger at workflows.py:84-85 rather than error, which is worse UX than preventing it. Note this trigger type is Atlas-fired, so it must not offer a Fire button (app.py:773-775 rejects firing it).                                                                                                                                    |
| Inspector footer Save/Delete (second persistence entry point)         | workflow-editor.tsx:1149-1164,698                                                                         | Destructive "Delete" calling removeSelected, and a "Save changes" button wired to the same top-level save() - so it writes the whole workflow and clears the global dirty flag despite its node-scoped label.                                                                                                       | Atlas can only accept whole-graph writes, so a per-node save is impossible; relabel the button "Save workflow" or remove it and leave saving to the single header Save (one write path is safer given the lost-update exposure). Delete stays local-until-save, but must also purge the node's layout entry (spec:617 "Layout entries for deleted nodes must be cleaned up") and any edges whose condition references it (manager_selected target / human_selected choice), otherwise the next save fails validation at workflows.py:214-227.                                                                                                                                                  |
| NODE_KINDS palette catalogue (9 kinds)                                | workflow-scaffold-store.ts:555-565                                                                        | Ordered array of {kind,label,hint} defining the nine scaffold palette kinds; only .label is read (as the new node's default name).                                                                                                                                                                                  | Shrink the catalogue to exactly four entries (worker, manager, join, human_gate). Drop `hint` entirely (it is dead) and stop using palette `label` as the node's persisted name - Atlas has no name/label field for worker/manager/join (workflow-definition.schema.json $defs/workerNode has additionalProperties:false and no label). The catalogue becomes display-only ordering.                                                                                                                                                                                                                                                                                                           |
| NODE_KINDS entry: decision                                            | workflow-scaffold-store.ts:559                                                                            | label "Ask to choose", hint "Let a person pick the next path".                                                                                                                                                                                                                                                      | Merge decision and approval into the single Atlas kind human_gate. "Ask to choose" is a human_gate WITH choices; "Approval" is a human_gate without. Do not keep two palette entries unless they both emit type:"human_gate".                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| NODE_KINDS entry: approval                                            | workflow-scaffold-store.ts:563                                                                            | label "Approval", hint "Human gate".                                                                                                                                                                                                                                                                                | One-time rename of the internal kind to human_gate (already mandated by docs/IMPLEMENTATION_PLAN.md Phase 3). Keep "Approval" only as the display label; do not keep `approval` as an API alias or round-trip mapping.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| DEFAULT_NODE_CONFIG: worker                                           | workflow-editor.tsx:58-63                                                                                 | { worker: "wrk_01", workspace: "thclaws", prompt: "...", output: "result" } with a hardcoded mock worker id.                                                                                                                                                                                                        | Rename keys to Atlas names (worker->worker_id, workspace->workspace_id\|workspace_key, output->outputs:[key]). Drop the hardcoded wrk_01 default - seed empty and populate from the live Atlas worker/workspace lists; Atlas re-checks references server-side (validate_workflow_references, atlas/app.py:1275). Validate the artifact-key pattern in the inspector.                                                                                                                                                                                                                                                                                                                           |
| DEFAULT_NODE_CONFIG: decision                                         | workflow-editor.tsx:69-75                                                                                 | { question, choices: [{id,label} x2] } - the only array-valued config; produces one source handle per choice.                                                                                                                                                                                                       | Keep the choices array 1:1 (ids/labels map exactly). Rename `question` to `reason` - it is the only Atlas field that can carry it. Enforce unique ids client-side, and require every outgoing edge to carry condition human_selected{choice} (atlas/workflows.py:238-239).                                                                                                                                                                                                                                                                                                                                                                                                                     |
| DEFAULT_NODE_CONFIG: join                                             | workflow-editor.tsx:78                                                                                    | { mode: "all", quorum: "2" } - quorum seeded regardless of mode, as a string.                                                                                                                                                                                                                                       | Keep mode with the same three values but make quorum an integer and emit it ONLY when mode === "quorum" - the seeded-always string breaks the schema on both counts. Also enforce quorum <= distinct incoming edges client-side (atlas/workflows.py:250-253).                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| DEFAULT_NODE_CONFIG: approval                                         | workflow-editor.tsx:79-83                                                                                 | { approvers: "ops-lead", message: "...", timeout_s: "600" } - timeout_s is a string.                                                                                                                                                                                                                                | Map message -> reason and keep label. Drop approvers and timeout_s: Atlas has no per-gate approver list or gate timeout; who may decide is governed by the approvals.decide permission (atlas/app.py:745, 1199-1200), and there is no gate expiry. Do not invent a UI field that cannot be saved.                                                                                                                                                                                                                                                                                                                                                                                              |
| DEFAULT_NODE_CONFIG: manager                                          | workflow-editor.tsx:84                                                                                    | { prompt: "..." } - single key, no model/temperature.                                                                                                                                                                                                                                                               | Keep prompt, add the mandatory schema field, and surface the routing fields the manager shares with worker. Manager is under-configured today, not over-configured.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Add a node from the palette (click only, random position)             | workflow-editor.tsx:343-359                                                                               | Click-to-add (no drag-and-drop); creates id n\_<5 random base36>, drops it at a random flow position, selects it, marks dirty.                                                                                                                                                                                      | Keep click-to-add for the four kinds. Replace the random position with a deterministic placement (spec section 13: layered left-to-right auto-layout, user-pinned positions preserved) written to the local layout store keyed by workflow id, never to Atlas. Keep collision-checked unique ids; consider readable ids since the id is the only stable node name Atlas stores. New nodes must seed their required Atlas fields (join.mode, manager.schema).                                                                                                                                                                                                                                   |
| Palette tile rendering and helper copy                                | workflow-editor.tsx:538-573                                                                               | w-64 sidebar, one 36px tile per kind with icon/label/description and a hover Plus; hidden below the lg breakpoint, so small screens cannot add nodes.                                                                                                                                                               | Rebuild with four tiles. Fix the `hidden lg:flex` dead end (a drawer or a bottom sheet below lg) and rewrite the footer copy, which currently promises palette drag-and-drop that does not exist.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Presentation: decision                                                | workflow-node-presentation.ts:45-51                                                                       | label "Ask to choose", ListChecks icon, teal tile/accent - the only teal kind.                                                                                                                                                                                                                                      | Fold into a single human_gate presentation. Either drop the separate identity, or vary icon/label at render time on whether choices.length > 0 (ListChecks with choices, ShieldCheck without) while both emit type human_gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Presentation: approval                                                | workflow-node-presentation.ts:73-79                                                                       | label "Approval", ShieldCheck icon, orange tile/accent - the only orange kind.                                                                                                                                                                                                                                      | Rekey the entry to human_gate while keeping "Approval" (or "Human gate") as the display label. This is the one-time rename the plan already mandates.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Presentation colour tokens violate the design-token rule              | workflow-node-presentation.ts:28-85                                                                       | Seven of nine kinds hardcode Tailwind palette literals; the node body/handles/dividers hardcode hex literals (#101b29, #91a4b7, #314154, #2b3b4d, #53677b, #b7c5d2).                                                                                                                                                | Fix while rewriting: CLAUDE.md forbids hardcoded colours and arbitrary literals. With only four kinds left, define four semantic tokens in src/styles.css (worker/manager/join/human-gate) plus surface/handle/divider tokens, and delete every hex and palette literal from workflow-node.tsx and the presentation map.                                                                                                                                                                                                                                                                                                                                                                       |
| Node title and hint line                                              | workflow-node.tsx:83-94                                                                                   | Kind tile + truncated 13px title from data.label + truncated 10px line from data.hint \|\| presentation.description.                                                                                                                                                                                                | The title can no longer be a free-text persisted name for worker/manager/join. Show the node id as the title for those three (it is the only identity Atlas stores) with the human_gate label used when present; keep the second line as a live-derived summary, not a stored string.                                                                                                                                                                                                                                                                                                                                                                                                          |
| Node hint is computed once at load and never refreshed                | workflow-editor.tsx:138                                                                                   | nodeHint() runs only in toFlow(); inspector edits update data.config but never data.hint, so the hint goes stale after the first edit.                                                                                                                                                                              | This is a live bug to fix, not carry over. Derive the summary line inside AtlasNode from the current node data at render time (or a memo on config), and stop storing `hint` in node data at all.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Target handle (one, left, suppressed for trigger)                     | workflow-node.tsx:76-82                                                                                   | One left target handle on every node except kind "trigger", which renders none.                                                                                                                                                                                                                                     | Every one of the four kinds keeps a single left target handle - the trigger suppression disappears with the trigger kind. Mark graph.start with a badge instead, and let the user re-point `start` at any node.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Per-kind branch handles: decision (one per choice)                    | workflow-node.tsx:21-38                                                                                   | Maps config.choices to source handles with id `choice:<id>`; non-array choices degrade to none.                                                                                                                                                                                                                     | Keep the one-handle-per-choice model for human_gate, rekeyed from `choice:` to the Atlas choice id. On connect, write edge condition {type:"human_selected", choice:<id>} automatically - Atlas rejects any other condition from a gate that declares choices.                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Branch row list rendering and handle positioning                      | workflow-node.tsx:98-117                                                                                  | Divider + 20px rows with an accent dot and label, each Handle absolutely positioned with inline top = 70 + index\*20 px hardcoded to the header height.                                                                                                                                                             | Keep the pattern for human_gate choices but stop hardcoding the offset - measure or use a CSS-variable row height so the handle cannot desync from its label. Also bound the list: choices has no max in the schema, so a gate with many choices must scroll or collapse rather than grow unbounded.                                                                                                                                                                                                                                                                                                                                                                                           |
| Run-state ring (border/glow) on the node                              | workflow-node.tsx:47-60                                                                                   | Six-way tone precedence running/waiting/success/failed/skipped/selected; run state always outranks selection.                                                                                                                                                                                                       | Retype the runState union to Atlas's actual node states. Map waiting_for_human onto the existing amber tone, add distinct tones for cancelled and interrupted, and drop "skipped" (skipping happens on EDGES - atlas/workflows.py:692 emits condition_skipped - so render it by dimming the edge, not the node). Keep the precedence rule.                                                                                                                                                                                                                                                                                                                                                     |
| Run-state badge dot                                                   | workflow-node.tsx:61-70,95                                                                                | 8px dot: pulsing primary/amber for running/waiting, success/destructive for terminal, static grey for everything else - queued and skipped are indistinguishable.                                                                                                                                                   | Same retyping as the ring. Give queued, cancelled and interrupted visually distinct treatments and add a title/aria-label carrying the literal Atlas state so status is not colour-only (accessibility).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Running progress bar                                                  | workflow-node.tsx:119-123                                                                                 | 2px track with a fixed 50%-width pulsing bar when runState is running - decorative, not tied to real progress.                                                                                                                                                                                                      | Keep only as an explicitly indeterminate indicator (full-width shimmer), never a partial fill that implies 50% done. Do not drive it from a timer; drive it purely from the node being in Atlas state `running`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| nodeHint(): worker                                                    | workflow-editor.tsx:111-114                                                                               | "Saves <output>" when output is set, else the workspace value, else "Worker job".                                                                                                                                                                                                                                   | Keep the shape but read outputs[0] instead of a scalar `output`, and fall back through worker_id -> role/tags routing -> "Unrouted" so an unroutable node is visibly incomplete before save.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| nodeHint(): decision                                                  | workflow-editor.tsx:116                                                                                   | Always `<n> choices for a person`, including "1 choices" and "0 choices".                                                                                                                                                                                                                                           | Keep for human_gate with correct pluralisation, and treat zero choices as a validation error rather than a hint (either omit the choices key entirely - a plain approval gate - or require >= 1).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| nodeHint(): approval                                                  | workflow-editor.tsx:117-120                                                                               | "For <approvers>" when approvers is set, else "Wait for review".                                                                                                                                                                                                                                                    | Drop the approver name from the hint - it cannot be stored. Hint from `reason` (truncated) or fall back to "Waits for a human decision".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| nodeHint(): join                                                      | workflow-editor.tsx:121-124                                                                               | "Wait for <mode>" using the raw mode value, else "Wait for branches".                                                                                                                                                                                                                                               | Keep, with friendly copy per mode: all -> "Waits for every branch", any -> "Continues on the first branch", quorum -> "Waits for N of M" using the live distinct-incoming count so the quorum > incoming rejection (atlas/workflows.py:250-253) is visible before save.                                                                                                                                                                                                                                                                                                                                                                                                                        |
| nodeHint(): manager (and fallback for any unhandled kind)             | workflow-editor.tsx:127                                                                                   | Unguarded final return showing config.prompt, falling back to "Choose a connected path" - catches any future kind with manager-flavoured copy.                                                                                                                                                                      | Keep the manager prompt hint but add an explicit `kind === "manager"` guard and make the default branch an exhaustiveness check that renders an "Unsupported node type" state. The current silent catch-all is exactly the fail-open behaviour the plan bans.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Node rename                                                           | workflow-editor.tsx:764-770,695,292-302                                                                   | Controlled "Step name" input bound to data.label; every keystroke rewrites node data and sets dirty. Does not re-derive the hint.                                                                                                                                                                                   | Split the field by kind. human_gate keeps a real, saved `label`. worker/manager/join get either an editable node ID (which IS the persisted name, subject to uniqueness at atlas/workflows.py:165-167 and to rewriting every referencing edge, graph.start, and manager_selected target) or a local-only display name stored alongside positions in the local layout record. Do NOT smuggle an unschema'd `label` key onto those three node types.                                                                                                                                                                                                                                             |
| Node delete                                                           | workflow-editor.tsx:361-369,745-752,1149-1156,241-248                                                     | Two entry points call removeSelected (drops the node plus every incident edge, clears selection, sets dirty), no confirmation. React Flow's Backspace path deletes via handleNodesChange WITHOUT setting dirty, so a keyboard delete can be lost without a warning.                                                 | Fix the dirty-tracking bug: set dirty for every remove change, including React Flow's own keyboard deletion. After a delete, re-run local validation - deleting the start node, orphaning a manager_selected target, or dropping incoming edges below a join's quorum must surface as a blocking error. Also clean up the deleted node's local layout entry (spec section 13: "Layout entries for deleted nodes must be cleaned up").                                                                                                                                                                                                                                                          |
| Decision choice editing rewires handles and edge labels               | workflow-editor.tsx:309-341,975-1020                                                                      | Editing the choices list rewrites config.choices, drops edges whose sourceHandle no longer matches a live choice, and relabels surviving choice edges. New choices seed label `Option <n+1>`; each row has a `Remove <label>` button.                                                                               | Keep this behaviour, it is exactly right and Atlas depends on it: removing a choice MUST prune its edges or the save is rejected. Rework it to rewrite edge condition.choice rather than an edge label (edges have no label field), and add client-side unique-id enforcement plus the empty-list guard from atlas/workflows.py:185-186.                                                                                                                                                                                                                                                                                                                                                       |
| Edge auto-labelling from the source node's branch config              | workflow-editor.tsx:258-287                                                                               | onConnect derives an edge label from the source kind (condition true/false labels, decision choice label); other kinds get an unlabelled edge. Edges styled primary at 65% opacity.                                                                                                                                 | Invert the mechanism: onConnect must auto-derive the CONDITION, not a label, and the displayed caption is rendered from that condition at draw time. Emit manager_selected{target: to} from a manager, human_selected{choice} from a gate handle, and `always` otherwise - the plan requires every edge to carry an explicit condition.                                                                                                                                                                                                                                                                                                                                                        |
| Nine palette kinds vs four Atlas node types (no save-time validation) | workflow-scaffold-store.ts:9-11                                                                           | The palette exposes nine kinds and nothing prevents building a graph Atlas cannot accept; Save writes only to the in-memory Zustand mock.                                                                                                                                                                           | Phase 3 must add a client-side validator mirroring atlas/workflows.py (four types only, start exists, edges reference live nodes, join quorum <= distinct incoming, cycles guarded, manager/gate edge conditions) AND surface Atlas's own error string at the offending node/edge. Use POST /api/workflows/{id}/validate as the pre-save dry run so the user sees Atlas's verdict without persisting.                                                                                                                                                                                                                                                                                          |
| Node position and viewport persistence                                | workflow-editor.tsx:343-359 (random x/y) and the save mapper at :368-380 (writes x/y into the mock store) | The scaffold stores x/y on each node in its mock workflow record, so layout survives inside the mock.                                                                                                                                                                                                               | Positions and viewport move to localStorage keyed by workflow id + graph version, with layered left-to-right auto-layout when no local layout exists, per-node cleanup on delete, and an explicit note in the UI that layout does not sync across devices or users. Never send x/y to Atlas.                                                                                                                                                                                                                                                                                                                                                                                                   |
| Run button (header)                                                   | workflow-editor.tsx:527-533                                                                               | Always-enabled header Play button calling the local run(); no validation, no disabled state, no in-flight guard.                                                                                                                                                                                                    | Button becomes a react-query mutation posting {workflow_definition_id, input} to POST /api/workflow-runs. It must be disabled while the mutation is in flight, disabled/hidden without the workflows.run permission (atlas/app.py:1201-1202), and must surface Atlas's synchronous 400 validation message at the offending node/edge instead of starting anything locally.                                                                                                                                                                                                                                                                                                                     |
| run() end-to-end sequence                                             | workflow-editor.tsx:396-455                                                                               | cancel simulator -> save() to mock store -> addRun() -> seed queued states + SYSTEM log -> updateRun -> construct and start the timer simulator over a node/edge snapshot.                                                                                                                                          | Collapses to: (optional) PUT /api/workflows/{id} if dirty -> POST /api/workflow-runs -> take run.id from the 202 body -> open the run view / subscribe. Steps 4-7 (seeding node states, seeding logs, overwriting the run record) all disappear: the client never writes run state. There is no local snapshot semantics either - Atlas executes the persisted graph, not the canvas.                                                                                                                                                                                                                                                                                                          |
| start(): choosing the entry nodes                                     | workflow-simulator.ts:200-207                                                                             | Entry set = trigger-kind nodes OR zero-incoming nodes, falling back to nodes[0]; every entry node starts, so multiple simultaneous roots are supported.                                                                                                                                                             | Replace the heuristic with one explicit graph.start node id. Multi-root starts and 'orphan node also starts' are impossible in Atlas - there is exactly one start. The editor needs a 'set as start' affordance and a badge on the start node, plus a pre-save error when start is unset or points at a deleted node. Array-order fallback must be deleted (the plan explicitly forbids treating array order as execution order).                                                                                                                                                                                                                                                              |
| startNode(): dedupe and gate branching                                | workflow-simulator.ts:158-198                                                                             | Each node runs at most once per run (started Set); approval/decision kinds become a pending gate instead of running; everything else goes running then a timer.                                                                                                                                                     | The at-most-once assumption is wrong for Atlas: guarded cycles and retries produce multiple attempts, each a separate runtime node row with its own attempt number. The node card must render the latest attempt state plus an attempt counter, and the run view must be able to list per-attempt rows from GET /api/workflow-runs/{id}. The gate branch collapses to the single human_gate kind.                                                                                                                                                                                                                                                                                              |
| deliver(): join-node barrier semantics                                | workflow-simulator.ts:78-99                                                                               | Join barrier by arrived-edge Set; threshold any=1, quorum=Number(config.quorum) default '2', else incoming count; a NaN quorum deadlocks the join forever.                                                                                                                                                          | Barrier execution moves to Atlas. What the editor keeps is the join inspector: a mode radio (all/any/quorum) that always emits mode, and an integer quorum input for quorum mode. Client-side pre-validation must reject a non-integer, <1, or > distinct-incoming quorum before save so the NaN deadlock class is unrepresentable rather than merely unlikely.                                                                                                                                                                                                                                                                                                                                |
| finishIfIdle() and end-of-run skipping                                | workflow-simulator.ts:55-67                                                                               | Finishes when nothing is active and no gate is pending; marks every un-started node 'skipped'; logs a hard-coded success line.                                                                                                                                                                                      | 'skipped' has no Atlas representation - a node that never ran simply has no runtime node row. Render that as a neutral 'not run' tone derived from row absence, and take the terminal state from run.state (which can be succeeded, failed, or cancelled), never assuming success.                                                                                                                                                                                                                                                                                                                                                                                                             |
| NodeRunState: the six states                                          | workflow-simulator.ts:5, workflow-node.tsx:11, workflow-scaffold-store.ts:100                             | queued\|running\|waiting\|success\|failed\|skipped, duplicated across three files.                                                                                                                                                                                                                                  | Retype to Atlas's vocabulary in ONE place in src/lib/ and import it everywhere: success->succeeded, waiting->waiting_for_human, add cancelled and interrupted, drop skipped (derive 'not run' from row absence). Unknown incoming states must fail closed to a neutral tone rather than crash. Also add the run-level union: queued/running/paused/waiting_for_human/recovery_required/succeeded/failed/cancelled (atlas/workflows.py:471,496,599,1164 and atlas/db.py:1203-1206).                                                                                                                                                                                                             |
| Node visual per run state (runTone + stateDot + progress bar)         | workflow-node.tsx:47-70, workflow-node.tsx:119-123                                                        | Border/shadow/dot/progress-bar styling per run state; queued, skipped and undefined all collapse to the same static grey dot.                                                                                                                                                                                       | Keep the visual language, re-key it to the Atlas state union, and give cancelled/interrupted their own tone instead of falling through to default. Also stop conflating queued with 'never ran' - they are different in Atlas. Replace the hardcoded hexes (#314154, #53677b) with tokens from src/styles.css per CLAUDE.md.                                                                                                                                                                                                                                                                                                                                                                   |
| Run states pushed onto canvas nodes                                   | workflow-editor.tsx:457-464                                                                               | useEffect rewrites every node's data.runState whenever runStates changes; clearing the run resets to undefined.                                                                                                                                                                                                     | Derive node run state in a useMemo that merges the layout/graph nodes with the Atlas runtime-node rows (keyed by node_key), instead of a setState-in-effect loop that rewrites every node on every tick. That also avoids re-rendering the whole canvas on each poll.                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Animated edge highlighting (dead in practice)                         | workflow-editor.tsx:143-152, workflow-editor.tsx:222                                                      | toFlow sets edge.animated from source=success && target=running, but toFlow only runs once in a useMemo with no runStates, so the flag is always false.                                                                                                                                                             | Delete the source/target state heuristic. Drive edge emphasis from edge_taken / condition_skipped events in the run event stream: taken edges get the active stroke, skipped edges get a dimmed/dashed stroke. Because the edge array must now be re-derived per run tick, memoize on the event seq, not on the whole event array.                                                                                                                                                                                                                                                                                                                                                             |
| PendingGate type: two kinds                                           | workflow-simulator.ts:7-20, workflow-simulator.ts:164-189                                                 | Discriminated union approval{message} \| decision{question, choices}; a decision with zero choices renders zero buttons and deadlocks the run.                                                                                                                                                                      | Collapse to a single Atlas approval model. The approve/reject vs choose branch is decided by whether approval.choices is non-empty, not by two node kinds. The zero-choice deadlock becomes unrepresentable: Atlas rejects an empty choices list at save (atlas/workflows.py:185-186). Fields rename: message->reason, title->label.                                                                                                                                                                                                                                                                                                                                                           |
| How a gate pauses the run                                             | workflow-simulator.ts:164-189, workflow-simulator.ts:61-62                                                | Sets a single module-level pending gate, node -> waiting, warn log, no timer and no timeout; two concurrent gates silently overwrite each other.                                                                                                                                                                    | The single 'pending' slot must become a list: GET /api/workflow-runs/{id} returns approvals[] (atlas/app.py:669) and a run accumulates approval rows across attempts, plus policy-level approvals with no graph node (atlas/workflows.py:949-960). Render every pending approval, keyed by approval id. Atlas also has no per-gate timeout, but it does have policy.max_minutes (docs/specs/workflow-definition.schema.json:247) - surface that instead of the scaffold's unused timeout_s field.                                                                                                                                                                                              |
| approve(approved) flow                                                | workflow-simulator.ts:209-224                                                                             | Local approve/reject: reject stops the run, fails the gate node, skips the rest, calls onFinish('failed') bypassing the finished flag; approve logs and fans out to all edges.                                                                                                                                      | Two server mutations behind server functions that validate the session and call the fixed Atlas operation. Must handle: 409-style 'approval already decided' (atlas/workflows.py:732-733), 'approval requires a branch choice' when choices exist (atlas/workflows.py:628-629), and the approvals.decide permission gate (atlas/app.py:1199-1200) for UX disabling only. After the 202, invalidate the run and approvals queries - do not optimistically write a terminal state.                                                                                                                                                                                                               |
| choose(choiceId) flow                                                 | workflow-simulator.ts:226-235                                                                             | Local choice: validates the id against the gate's choices, logs a curly-quoted line, routes only down the matching choice edge.                                                                                                                                                                                     | Becomes a server function posting {choice: <choice id>}. Client-side id validation is redundant but keep the button disabled state; the authoritative rejection is Atlas's. Routing after the choice is Atlas's (\_continue_human_gate_decision, atlas/workflows.py:670-706) - the UI just refetches.                                                                                                                                                                                                                                                                                                                                                                                          |
| cancel()                                                              | workflow-simulator.ts:237-242                                                                             | Sets stopped, clears pending/active, never calls onFinish, so the store run stays 'running' forever - a cancelled run is never marked cancelled.                                                                                                                                                                    | Split the one concept into two: 'stop watching' (pure UI - unsubscribe/close the drawer, no server call) and 'Cancel run' (a destructive Atlas mutation, needs confirmation and the workflows.run permission). The scaffold's silent orphaning bug disappears because Atlas is the one finalizing state.                                                                                                                                                                                                                                                                                                                                                                                       |
| RunGate approval UI                                                   | workflow-editor.tsx:172-196, workflow-editor.tsx:656-660                                                  | Amber card with title/message and Reject / Approve buttons wired straight to the simulator, no confirmation.                                                                                                                                                                                                        | Keep the card; bind title->approval.label, body->approval.reason, and the buttons to the approve/reject mutations with pending/disabled states and an inline error slot. Add a confirmation for Reject (it fails the whole run). Hide or disable both when the identity lacks approvals.decide, and re-token the amber literals against src/styles.css.                                                                                                                                                                                                                                                                                                                                        |
| RunGate decision UI                                                   | workflow-editor.tsx:199-218                                                                               | Teal card with a question and one button per choice; zero choices renders an empty grid.                                                                                                                                                                                                                            | Same card, fed from approval.choices, buttons calling the choose mutation. The empty-grid case is dead: Atlas cannot store a gate with an empty choices array, so the editor should also refuse to save one. Show the question text from approval.reason (there is no separate 'question' field in Atlas).                                                                                                                                                                                                                                                                                                                                                                                     |
| Log drawer header controls                                            | workflow-editor.tsx:616-652                                                                               | Chevron toggle, a forever-pulsing dot with 'Live run {id}', an 'Open full view' link to /runs/$id, and a Clear button that cancels the simulator and wipes UI state while leaving the store run untouched.                                                                                                          | The pulsing dot must reflect run.state (pulse only while running, amber for waiting_for_human, static for terminal). 'Open full view' now carries a real Atlas run id. Clear becomes 'Close' (UI only) and a separate explicit 'Cancel run' action appears for non-terminal runs; the current conflation must not survive.                                                                                                                                                                                                                                                                                                                                                                     |
| Log line rendering and levels                                         | workflow-editor.tsx:653-682, workflow-editor.tsx:669-676                                                  | Monospace rows: timestamp, w-28 truncated [NODE], text; colour by level, with 'warn' rendered identically to 'info'; React keys are array indices.                                                                                                                                                                  | Re-shape the row to timestamp (created_at) + node_key + a rendered event_type/payload. Severity becomes a local mapping table from event_type (node_failed/guard_tripped/recovery_required -> error; approval_created -> warn; node_succeeded/run_finished(succeeded) -> success; rest -> info) and warn must get its own distinct token colour. Key rows by seq, which is stable and unique per run, never by index.                                                                                                                                                                                                                                                                          |
| Run implies Save (side effect)                                        | workflow-editor.tsx:371-399                                                                               | run() silently calls save() first, committing all unsaved canvas edits (including accidental drags) and clearing the dirty flag.                                                                                                                                                                                    | Make the coupling explicit instead of silent. When dirty, Run must either be disabled with 'Save to run your changes', or offer an explicit 'Save and run' that shows what will be saved and can fail on Atlas validation before any run is created. Also: node positions are NOT part of a save (Atlas has no layout endpoint - docs/specs/workflow-visual-builder-spec-en.md:598-621), so a drag must dirty local layout storage only, never the Atlas definition.                                                                                                                                                                                                                           |
| Inspector panel shell                                                 | workflow-editor.tsx:688-700                                                                               | Right-hand 340px aside rendered only when a node is selected; per-kind Field stack fed by mock workers/workflows props.                                                                                                                                                                                             | Keep the aside shell, but it can no longer be node-only: it becomes a context inspector with node / edge / policy / trigger / workflow modes. Only four node modes remain (worker, manager, join, human_gate). The `workers` prop must come from Atlas Fleet (live), and the `workflows` prop from the Atlas workflow list, not from the mock Zustand store. Header subtitle should show the node id + validation state, not the static 'Configure step'.                                                                                                                                                                                                                                      |
| Step name field (all kinds)                                           | workflow-editor.tsx:764-770                                                                               | Plain text input writing data.label for every node kind.                                                                                                                                                                                                                                                            | Split into two things. For human*gate, keep it as the Atlas `label` field. For worker/manager/join there is no label in the semantic JSON at all: the inspector's identity field must edit the node `id` (recommended pattern ^[A-Za-z*][A-Za-z0-9_]\*$, spec §6 rules 1-3), with an atomic rename that rewrites graph.start, every edge from/to, manager_selected.target and max_iterations_below.node. Any friendly display name for worker/manager/join must live in local layout state, never in graph.nodes.                                                                                                                                                                              |
| Worker: prompt help text                                              | workflow-editor.tsx:782-785                                                                               | Help line telling users to reference earlier results as {{ trigger.payload }}.                                                                                                                                                                                                                                      | Keep the help line but fix the syntax: Atlas interpolates {artifact.KEY}, not {{ trigger.payload }}. Ideally list the artifact keys actually produced by upstream nodes (their `outputs[0]`) so the hint is real instead of decorative.                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Worker: worker picker                                                 | workflow-editor.tsx:787-800                                                                               | Select writing config.worker from a hardcoded 'auto' option plus the five mock initialWorkers.                                                                                                                                                                                                                      | Rewrite the option list to load live Atlas workers via a server function (GET /api/workers), not the mock store. Selecting a worker writes `worker_id`; the 'Auto' option must OMIT worker_id entirely (routing then falls back to role/tags/hints per spec §7.1 'workspace_id -> worker_id -> auto-route'), it must not write the literal string 'auto'. Grey out workers excluded by policy.allowed_worker_ids and surface Atlas's reference-validation error at this field.                                                                                                                                                                                                                 |
| Worker: workspace field                                               | workflow-editor.tsx:802-808                                                                               | Free-text monospace input writing config.workspace, default 'thclaws'.                                                                                                                                                                                                                                              | Free text is unsafe here. Replace with a workspace picker scoped to the selected worker, writing `workspace_id`; keep a separate advanced `workspace_key` text input for the routing-hint case. Note the precedence rule (workspace_id wins over worker_id) in the UI, and validate ownership client-side so users do not discover it only as a 400 from Atlas.                                                                                                                                                                                                                                                                                                                                |
| Worker: 'Save result as' output field                                 | workflow-editor.tsx:809-817                                                                               | Monospace input writing config.output, sanitised to [a-zA-Z0-9_] on every keystroke.                                                                                                                                                                                                                                | Becomes `outputs: [key]` - an array of exactly one key, omitted entirely when empty (spec §7.1: without outputs the job succeeds but creates no artifact; say that in the UI). Fix the sanitiser: it currently allows a leading digit, which the artifactKey pattern rejects. This key is also what edge conditions (artifact_equals / artifact_in) and {artifact.KEY} prompt references point at, so the edge inspector must read the upstream node's outputs[0].                                                                                                                                                                                                                             |
| Worker: Work budget (minutes)                                         | workflow-editor.tsx:833-843                                                                               | Number input writing config.budget_minutes; clearing the field writes 0.                                                                                                                                                                                                                                            | Per-node minutes do not exist in Atlas. Re-label the field 'Work budget (units)' writing `budget_units`, enforce integer >= 1 (Number('') -> 0 currently produces a value Atlas rejects at save time), and omit the key when unset rather than persisting a fallback. The minutes concept moves to the new policy panel as `max_minutes`.                                                                                                                                                                                                                                                                                                                                                      |
| Worker: Require structured output toggle                              | workflow-editor.tsx:844-866                                                                               | Custom switch writing boolean config.structured_output.                                                                                                                                                                                                                                                             | Keep the switch UI but serialize it as `output_format: "json"` written ONLY when on, and delete the key when off - never emit a boolean. Warn in the sub-text that JSON mode fails the node if the worker returns anything unparseable, and that it is meaningless without an `outputs` key set.                                                                                                                                                                                                                                                                                                                                                                                               |
| Trigger: mode switcher                                                | workflow-editor.tsx:874-892                                                                               | Four RadioChoice rows writing config.mode on a trigger NODE (webhook/schedule/manual/event).                                                                                                                                                                                                                        | The trigger node is deleted from the palette and from graph.nodes. The mode switcher survives only inside a new trigger panel bound to the /api/workflow-triggers resource, where it selects `type`. The four existing modes map to manual / webhook / schedule / workflow_run_completed; each trigger also needs `name` and `enabled`, which the scaffold never had. What the canvas keeps instead is a Start badge on graph.start.                                                                                                                                                                                                                                                           |
| Trigger: schedule mode - Time of day                                  | workflow-editor.tsx:893-901                                                                               | input type=time writing config.time, default 09:00.                                                                                                                                                                                                                                                                 | Moves to the trigger panel as `config.daily_time`; the HTML time input already produces the required HH:MM 24h form. Add the second, currently-missing schedule shape: `interval_minutes` (number, >= 1/60) as an either/or with daily_time, since the config is a strict oneOf and sending both is rejected.                                                                                                                                                                                                                                                                                                                                                                                  |
| Trigger: event mode - Start after workflow                            | workflow-editor.tsx:910-927                                                                               | Select writing config.event_workflow from mock store workflows minus the current one.                                                                                                                                                                                                                               | Becomes a trigger of type `workflow_run_completed` with config.source_workflow_definition_id, options loaded from Atlas. Add the `state` filter the scaffold lacks (the help text's 'completes successfully' is currently a lie - without a state filter it fires on any completion). Surface the runaway guards: Atlas blocks self-triggering and A->B->A cycles and caps the chain at MAX_TRIGGER_CHAIN_DEPTH=20 (atlas/workflows.py:76-88), so excluding only the current workflow from the list is not the whole rule.                                                                                                                                                                     |
| Trigger: manual mode - PanelHint                                      | workflow-editor.tsx:928-930                                                                               | Hint text: use the Run button in the editor or API.                                                                                                                                                                                                                                                                 | Keep a manual-trigger row in the trigger panel, but rewrite the hint: Run now creates a real Atlas run via POST /api/workflow-runs (returning a real run id), not a timer simulation. Mention that Run requires the workflows.run permission and that the run may immediately enter waiting_for_human.                                                                                                                                                                                                                                                                                                                                                                                         |
| Decision: Question textarea                                           | workflow-editor.tsx:967-974                                                                               | 3-row textarea writing config.question, shown by the simulator's gate panel.                                                                                                                                                                                                                                        | The `decision` kind merges into `human_gate` (the choice form). The question becomes the gate's `reason`, with `label` as its short title - so this Field splits into two inputs. The text is real now: Atlas copies label/reason onto the approval record surfaced by /api/approvals, so it is what an approver actually reads.                                                                                                                                                                                                                                                                                                                                                               |
| Decision: choices editor - edit a choice label                        | workflow-editor.tsx:975-996                                                                               | Per-choice text input; editing rewrites config.choices and relabels edges whose sourceHandle is choice:<id>.                                                                                                                                                                                                        | Label editing maps 1:1 to human_gate.choices[].label - keep it, but reject empty labels client-side (Atlas rejects them). The edge-relabelling half must be rewritten: edges have no label, so what binds a choice to an edge is `condition: {type:'human_selected', choice:'<id>'}`. Renaming a choice ID must atomically refactor every human_selected.choice reference (spec §7.4).                                                                                                                                                                                                                                                                                                         |
| Decision: choices editor - delete a choice                            | workflow-editor.tsx:997-1007                                                                              | Per-row X, disabled below 2 choices; deleting prunes edges on the choice:<id> handle.                                                                                                                                                                                                                               | The floor is 1, not 2 (and dropping to zero choices is legal - it turns the gate into the approve/reject form). Change the disabled condition to choices.length <= 1 and offer an explicit 'remove all choices / use approve-reject' path. Deleting a referenced choice must be blocked or must delete its edges in the same action with confirmation (spec §7.4), not silently prune.                                                                                                                                                                                                                                                                                                         |
| Decision: choices editor - add a choice                               | workflow-editor.tsx:1010-1024                                                                             | Appends a choice with a 6-char random id and a count-based label; no uniqueness check, no cap.                                                                                                                                                                                                                      | Keep the button, but generate ids deterministically-unique against the existing set (and ideally let the user type a readable id, since the id is what appears in every human_selected edge condition). Duplicate labels after deletions are only cosmetic; duplicate ids are a hard Atlas 400. No cap is needed - Atlas imposes none.                                                                                                                                                                                                                                                                                                                                                         |
| Decision: ports PanelHint                                             | workflow-editor.tsx:1027-1030                                                                             | Hint: each choice becomes an output port to connect.                                                                                                                                                                                                                                                                | Keep the port model - it matches Atlas well - but reword to state the hard rule: every outgoing edge from a gate with choices MUST be human_selected referencing a declared choice, otherwise the save is rejected. Add a validation marker for a declared choice with no outgoing edge (a dead end at runtime).                                                                                                                                                                                                                                                                                                                                                                               |
| Decision: choices default seed                                        | workflow-editor.tsx:69-75, workflow-editor.tsx:92-93                                                      | New decision nodes seed two choices (choice_a/choice_b); configChoices returns [] for non-array config.                                                                                                                                                                                                             | A new human_gate should default to NO choices (the approve/reject form, spec §7.4) and offer 'Add choices' to convert it to the choice form. Keep configChoices' defensive [] but treat absent-vs-empty as meaningful: absent = approve/reject, present = must have >= 1 and must have human_selected edges.                                                                                                                                                                                                                                                                                                                                                                                   |
| Approval: Message to the approver                                     | workflow-editor.tsx:1036-1042                                                                             | 4-row textarea writing config.message.                                                                                                                                                                                                                                                                              | Rename the config key to `reason` as part of the one-time approval -> human_gate migration, and add the missing `label` input. Both are real Atlas fields that show up on the approval record, so they should be required-ish in the UI rather than silently defaulting to 'Workflow reached a human gate'.                                                                                                                                                                                                                                                                                                                                                                                    |
| Join: quorum count                                                    | workflow-editor.tsx:1085-1094                                                                             | Text input writing config.quorum as a string when mode is quorum; no validation.                                                                                                                                                                                                                                    | Must become a number input serialized as an integer - the current string '2' is rejected by validate_workflow_graph. Add the check the editor has never had: clamp/validate quorum against the number of DISTINCT incoming edge sources on the canvas (duplicates from one source count once) and show the error inline. Also drop the `quorum` key entirely when the user switches back to all/any (schema joinNode's all/any branch is additionalProperties:false, so a leftover quorum fails the canonical profile).                                                                                                                                                                        |
| Inspector header delete-node button                                   | workflow-editor.tsx:745-752, workflow-editor.tsx:361-369                                                  | Trash icon removing the node and every incident edge, no confirm, no undo.                                                                                                                                                                                                                                          | Add a confirmation naming the incident edges that will go, block deletion of the node referenced by graph.start until a replacement start is chosen, and cascade the reference cleanup the scaffold ignores: manager_selected.target and max_iterations_below.node pointing at the deleted node must be repaired or the save will be rejected. Wire it into an undo stack.                                                                                                                                                                                                                                                                                                                     |
| Inspector footer Delete button                                        | workflow-editor.tsx:1150-1156                                                                             | Duplicate full-width destructive Delete in the sticky footer, same handler.                                                                                                                                                                                                                                         | Same treatment as the header button, and pick one: two unconfirmed destructive entry points for one action is a hazard. Recommend keeping only the header icon and giving the footer to Save/Validate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Inspector footer 'Save changes' button                                | workflow-editor.tsx:1157-1163, workflow-editor.tsx:371-394                                                | Runs the whole-canvas save into the mock Zustand store (nodes with x/y + edges), clears dirty; in-memory only.                                                                                                                                                                                                      | Rewrite as a server-function PUT /api/workflows/{id} sending {name, description?, graph:{start,nodes,edges}, policy} only - x/y must be stripped and written to localStorage keyed by workflow id + graph version. Save becomes async with pending/error states, must run validation first, and needs the client-side lost-update guard called for in the plan (Atlas has no ETag/If-Match). Keep it as a whole-definition save (Atlas has no per-node PATCH), but the button label should stop implying it saves only this node.                                                                                                                                                              |
| Config write path (onConfigChange)                                    | workflow-editor.tsx:304-307                                                                               | Shallow-merges {key: value} into the selected node's free-form config bag and sets dirty.                                                                                                                                                                                                                           | The untyped string-keyed config bag has to go. Model each node kind as a discriminated union of exactly the Atlas fields, keep an adapter in src/lib/ that maps UI state -> semantic JSON (dropping UI-only keys), and let onConfigChange be a typed setter. Leftover scaffold keys (expr, branches, collection, approvers, timeout_s, path, mode) must be dropped at the boundary, not forwarded.                                                                                                                                                                                                                                                                                             |
| Config read path (value helper / defaultedConfig)                     | workflow-editor.tsx:87-101, workflow-editor.tsx:730-731                                                   | Two layers of defaulting; a default shown in the UI is not necessarily present in the saved config until the field is edited.                                                                                                                                                                                       | Display-defaulting is now a correctness bug, not a cosmetic one: join.mode, manager.schema and every edge condition must be emitted explicitly regardless of whether the user touched the control, while output_format and budget_units must be omitted when unset. Split 'default for display' from 'default for serialization' in the adapter and cover it with the round-trip fixtures the plan requires before wiring save.                                                                                                                                                                                                                                                                |
| Field / RadioChoice / PanelHint primitives                            | workflow-editor.tsx:1169-1218                                                                             | Label wrapper, fake-radio button row, and tinted hint box used throughout the inspector.                                                                                                                                                                                                                            | Reusable as-is visually, but fix two things while rewriting: Field wraps radio groups and multi-input grids in a single <label>, which is invalid for grouped controls (use fieldset/legend with role=radiogroup), and RadioChoice needs real keyboard semantics. Also check hit targets against the 44px minimum before Phase 3 ships.                                                                                                                                                                                                                                                                                                                                                        |

---

## 4. Returns as-is (23)

| Capability                                              | Where it lives                      | What it did                                                                                                                                                                           | Phase 3                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Pan and zoom — all library defaults, nothing configured | workflow-editor.tsx:579-594         | No pan/zoom/selection props are passed at all, so @xyflow/react v12 defaults apply (drag-pane pan, scroll zoom, shift-box-select, ctrl multi-select).                                 | Carry over unchanged; Atlas is indifferent. The only Phase 3 obligation attached to it is the accessibility rule at spec:133-134 — every drag interaction needs a keyboard/menu equivalent and 44x44px hit targets, which defaults alone do not give.                                                                                                                                                                           |
| No grid snapping                                        | workflow-editor.tsx:579-595         | snapToGrid/snapGrid never passed; dragging is free-form at 1px and the 22px Background gap is decorative only.                                                                        | No Atlas consequence either way. Optional polish: enable snapGrid={[22,22]} so the visible grid means something, since Phase 3 already owns layout locally.                                                                                                                                                                                                                                                                     |
| Back link to /workflows                                 | workflow-editor.tsx:470-476         | 36px icon-square TanStack Link with ArrowLeft, aria-label "Back to workflows", navigating unconditionally with no unsaved-changes guard.                                              | Keep the link as-is (the /workflows route exists at src/routes/\_app/workflows.index.tsx). But once Save is a real PUT /api/workflows/{id} (atlas/app.py:592-599) the silent-discard becomes destructive of server-bound work, so Phase 3 must add a TanStack useBlocker on semantic dirtiness (layout-only dirtiness should not block, since layout never reaches Atlas).                                                      |
| Keyboard shortcuts                                      | workflow-editor.tsx:579-594         | None defined; only React Flow defaults (Backspace/Delete, shift-drag selection, scroll zoom), and keyboard deletion does not set dirty.                                               | Keep React Flow defaults but route deletion through the editor's own handler so it marks semantic-dirty (the current handleNodesChange at line 244 only inspects "position"). Optionally add Cmd/Ctrl-S -> save and Cmd/Ctrl-Enter -> validate. The builder spec also asks for undo/redo over add/delete/connect/disconnect/field editing (docs/specs/workflow-visual-builder-spec-en.md:137), which this editor has never had. |
| Canvas hint pill                                        | workflow-editor.tsx:607-610         | pointer-events-none xl-only pill reading "Canvas / drag to arrange / connect ports to link".                                                                                          | Carry over unchanged, or drop it as noise. It has no Atlas coupling. If kept, it should be dismissible rather than permanently occupying the canvas corner.                                                                                                                                                                                                                                                                     |
| Log drawer toggle                                       | workflow-editor.tsx:618-626,229,418 | 28px ChevronRight button rotating 90deg when open, flipping drawerOpen; unmounts the body when collapsed; defaults open and is force-reopened on Run. No aria-label or aria-expanded. | Keep as-is, add aria-label and aria-expanded, and consider persisting the open/closed preference locally alongside the layout state. Unmount-on-collapse becomes more valuable, not less, once the body holds a live event stream.                                                                                                                                                                                              |
| NODE_KINDS entry: worker                                | workflow-scaffold-store.ts:557      | label "Worker Job", hint "Run a prompt on a worker".                                                                                                                                  | Keep. Align the palette label with the presentation label (one string, "Worker task") so the tile and the created node agree; the label no longer seeds a persisted node name.                                                                                                                                                                                                                                                  |
| NODE_KINDS entry: join                                  | workflow-scaffold-store.ts:562      | label "Join", hint "All / Any / Quorum".                                                                                                                                              | Keep. The dead hint text is actually the correct mode enum - promote it into the join inspector as a real mode selector instead of a hint string.                                                                                                                                                                                                                                                                               |
| NODE_KINDS entry: manager                               | workflow-scaffold-store.ts:564      | label "Manager", hint "LLM proposes next actions".                                                                                                                                    | Keep. addNode for manager must always seed schema:"manager_decision_v1" - Atlas rejects any other value, and the plan requires it always be emitted.                                                                                                                                                                                                                                                                            |
| Presentation: worker                                    | workflow-node-presentation.ts:31-37 | label "Worker task", Cpu icon, primary-token tile and accent.                                                                                                                         | Keep as-is; it already uses design tokens rather than palette literals.                                                                                                                                                                                                                                                                                                                                                         |
| Presentation: join                                      | workflow-node-presentation.ts:66-72 | label "Join", Merge icon, emerald tile/accent.                                                                                                                                        | Keep, but move emerald onto a design token from src/styles.css. Consider showing the mode (all/any/N) in the tile since it is now a required field.                                                                                                                                                                                                                                                                             |
| Presentation: manager                                   | workflow-node-presentation.ts:80-86 | label "Manager", Sparkles icon, primary-token tile/accent (shared with worker).                                                                                                       | Keep. Manager and worker sharing `primary` is fine and honest - both dispatch jobs to workers - but give manager a distinguishable accent shade so a manager's mandatory manager_selected edges are visually traceable.                                                                                                                                                                                                         |
| Node card shell rendering                               | workflow-node.tsx:72-75             | Fixed w-60 rounded-xl card on #101b29 with shadow, hover lift, 150ms transition; a `group` so handles react to hover.                                                                 | Keep the shell and interaction, swapping the hardcoded background for a token. The fixed width is worth keeping because it makes deterministic auto-layout (spec section 13) simple.                                                                                                                                                                                                                                            |
| Default single source handle (right)                    | workflow-node.tsx:124-130           | One right source handle for every kind without branch options; fanout and loop get only this one handle.                                                                              | Keep exactly as-is for worker, join, and choice-less human_gate: one handle that accepts many outgoing edges IS Atlas fan-out. Manager needs it too, but every manager edge must carry manager_selected with target == edge.to (atlas/workflows.py:224-230), so the UI must author that condition on connect.                                                                                                                   |
| Node selection                                          | workflow-editor.tsx:585-586         | Canvas click sets the editor's `selected` id (opens inspector); pane click clears it. React Flow's own selection drives the ring independently.                                       | Keep, but unify the two sources of truth - derive the inspector target from React Flow's selection so the ring and the panel can never disagree. Never persist selection to Atlas.                                                                                                                                                                                                                                              |
| Log drawer: visibility and height                       | workflow-editor.tsx:612-615         | Drawer renders only when runId is set; animated height across h-11 / h-52 / h-72 (taller while a gate is pending).                                                                    | Carry over as-is. Only the trigger changes: it opens on a real Atlas run id (and can also open for a run started elsewhere), and the tall variant keys off 'there is at least one pending approval' rather than a single pendingGate slot.                                                                                                                                                                                      |
| Worker: prompt textarea                                 | workflow-editor.tsx:774-786         | 5-row textarea writing config.prompt with a default seed string.                                                                                                                      | Map straight to node.prompt. Drop the DEFAULT_NODE_CONFIG placeholder text from the persisted value (Atlas permits empty prompt but the spec §7.1 table says the UI should warn) - show a warning rather than silently saving 'Describe the work this node should complete.'                                                                                                                                                    |
| Worker: Advanced worker settings disclosure             | workflow-editor.tsx:819-830         | Local useState toggle revealing the advanced block; never persisted.                                                                                                                  | Keep as-is (local useState, not persisted, not sent to Atlas). It must grow to hold the fields Atlas actually supports and the editor never had: role, tags, model, company, workspace_key, budget_units, execution, collect_files.                                                                                                                                                                                             |
| Approval: pause PanelHint                               | workflow-editor.tsx:1060-1062       | Hint: the run pauses here until an approver accepts or rejects.                                                                                                                       | Accurate as written - keep it. Strengthen it slightly: reject FAILS the run, it does not take an alternate path, and with choices the buttons become the choice labels instead of Approve/Reject.                                                                                                                                                                                                                               |
| Join: mode switcher                                     | workflow-editor.tsx:1066-1084       | Three RadioChoice rows writing config.mode: all / any / quorum.                                                                                                                       | Straight 1:1 map to join.mode. One serialization rule to add: `mode` must ALWAYS be written (spec line 173), so it cannot rely on the DEFAULT_NODE_CONFIG-only defaulting described in the 'Config read path' item - the adapter must emit mode explicitly even when the user never touched the control.                                                                                                                        |
| Manager: prompt textarea                                | workflow-editor.tsx:1134-1141       | 5-row textarea writing config.prompt; the manager's only field.                                                                                                                       | Maps 1:1 to manager.prompt. The adapter must always emit the required `schema: "manager_decision_v1"` alongside it (never user-editable). The prompt UI should state the manager must return manager_decision_v1 JSON ({stop, reason, next[]}) or the node fails, and the inspector should gain the same routing/budget/execution fields as a worker (schema lines 65-79) which it currently lacks.                             |
| Manager: PanelHint                                      | workflow-editor.tsx:1142-1144       | Hint: the manager can choose only among connected steps.                                                                                                                              | Keep - it is true. Add the enforcement it implies: when the source node is a manager, the edge inspector must force condition type manager_selected and auto-set target = edge.to; anything else is a hard save failure.                                                                                                                                                                                                        |
| Inspector close button                                  | workflow-editor.tsx:753-760         | X button clearing selection and unmounting the aside; edits already applied stay.                                                                                                     | Carry over unchanged. Selection must never be sent to Atlas and never be part of the dirty/save payload.                                                                                                                                                                                                                                                                                                                        |

---

## 5. New work — Atlas supports it, the editor never did (30)

This is the section to read if the worry is "will it be complete". The old editor was _missing_
most of Atlas's workflow model: it could not author a single edge condition, had no policy panel,
no validation, no start-node selection, no artifact handling, and no trigger management.

| Gap                                                                                                               | Where the gap is                                                                                             | Today                                                                                                                                                                                                                                         | Atlas support                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Phase 3 must add                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ----------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Edge labels are NOT directly editable anywhere in the UI                                                          | workflow-editor.tsx:934-963, 975-1026                                                                        | No edge inspector, no onEdgeClick/onEdgeDoubleClick, no edgeTypes, no EdgeLabelRenderer; edge text is only reachable indirectly through condition/decision node config. Selecting an edge only bumps stroke opacity (src/styles.css:217-219). | workflow-definition.schema.json:204 makes `condition` a REQUIRED edge field with six allowed shapes (:142-201); workflow-visual-builder-spec-en.md:94 lists the inspector as serving "the selected workflow, node, edge, policy, or trigger".                                                                                                                                                                                                                                            | Build the edge inspector — it is the single biggest missing surface. It must author all six condition types: always, artifact_equals (artifact + optional path + value), artifact_in (artifact + values[]), manager_selected (target forced to edge.to), human_selected (choice picked from the source gate's declared ids), max_iterations_below (node + max). Add onEdgeClick + selection state, and per spec:119 give the selected edge a focus treatment that is not colour-only (styles.css:217-219 currently is).                                                                                                                                            |
| Viewport / zoom persistence: none                                                                                 | workflow-editor.tsx:579-594, workflow-scaffold-store.ts:78-89                                                | No defaultViewport, onViewportChange, or onMoveEnd; no viewport field on the Workflow type and no localStorage, so pan/zoom resets to fitView on every remount.                                                                               | workflow-visual-builder-spec-en.md:600-611 recommends layout state {layout_version, nodes:{id:{x,y}}, viewport:{x,y,zoom}}; :82 and atlas/db.py:1065-1087 confirm Atlas stores none of it (no layout column, no layout endpoint).                                                                                                                                                                                                                                                        | Add local layout persistence in the shape the spec recommends, keyed by workflow id + graph version, including viewport. Be explicit in the UI/docs that this is device-local and does not sync between users (IMPLEMENTATION_PLAN.md:106) — a shared workflow will look different to each viewer, and that is an Atlas constraint, not a bug.                                                                                                                                                                                                                                                                                                                     |
| Auto-layout: none                                                                                                 | workflow-editor.tsx:343-359                                                                                  | No layout library or function anywhere in src/components/atlas; new nodes are dropped at a random flow-space point (x 240-520, y 120-400), so they can land off-screen on a panned canvas and stack on each other.                            | workflow-visual-builder-spec-en.md:623-624 — auto-layout should default to a layered left-to-right arrangement, preserve user-pinned positions, and route loops around the top or bottom; :637 import step 8 "Generate layout for nodes with no position"; :616 "A node without a stored position uses auto-layout"; :91 lists auto-layout in the toolbar.                                                                                                                               | Auto-layout stops being optional the moment workflows arrive from Atlas: Atlas returns graphs with no coordinates at all (workflow-definition.schema.json:21-55 has no x/y), so any workflow created via API, AI draft, pack import, or another device opens with zero layout. Add a layered LTR layout (dagre/elk), run it for nodes missing a local position, expose it as a toolbar action, and place new nodes deterministically near the viewport centre via screenToFlowPosition instead of at random.                                                                                                                                                       |
| No connection validation                                                                                          | workflow-editor.tsx:258-287, workflow-node.tsx:76                                                            | isValidConnection, connectionMode and onConnectStart/End are absent; self-loops, duplicate parallel edges, many-into-one and unbounded fan-out are all allowed. The only structural guard is that trigger nodes render no target handle.      | atlas/workflows.py:224-225 (manager source requires manager_selected), :231-239 (human_selected requires a human_gate source and a declared choice; a gate with choices requires human_selected on every outgoing edge), :252-254 (join quorum must not exceed distinct incoming upstream count), :256-257 (a cycle requires policy.max_iterations or a max_iterations_below guard); workflow-visual-builder-spec-en.md:681 — duplicate exact edges are warned or blocked in the editor. | This is the largest genuinely new surface. Implement isValidConnection for what is knowable at drag time (duplicate from/to, self-loop warning) plus a full validator pass mirroring workflows.py:149-233 that runs before save/run and reports {severity, node/edge/field, message} against the affected element (spec:96, IMPLEMENTATION_PLAN.md:113). Note the cycle rule is not a connect-time block: a back-edge is legal, it just forces policy.max_iterations or a max_iterations_below condition — so the UI should offer to add the guard rather than refuse the edge.                                                                                    |
| "Check" button (dead control)                                                                                     | workflow-editor.tsx:514-519                                                                                  | xl-only "Check" button with a Wrench icon, no onClick, no disabled state, no validation logic behind it.                                                                                                                                      | atlas/app.py:607-616 (POST /api/workflows/{id}/validate -> {"ok": true} or a ValueError message); atlas/workflows.py:149-233 (validate_workflow_graph: node types, join mode/quorum, human_gate choices, edge conditions, cycle guard); atlas/workflows.py:304-349 (validate_workflow_references: unknown worker_id, workspace ownership, policy allow-lists)                                                                                                                            | Wire it for real: POST /api/workflows/{id}/validate with the serialized {graph, policy}, plus a local pre-flight mirroring validate_workflow_graph so obvious errors do not need a round-trip. Atlas returns a single flat ValueError string (e.g. "workflow join node j1 quorum exceeds distinct incoming upstream count"), so Phase 3 needs a parser mapping the embedded node id / edge index back to the offending node or edge - the plan's exit criterion is "Atlas validation errors appear at the relevant node/edge/form field" (docs/IMPLEMENTATION_PLAN.md Phase 3). Make it a real button with pending/ok/error states and show it at all breakpoints. |
| No route renders this editor                                                                                      | workflow-editor.tsx:221                                                                                      | Nothing imports WorkflowEditor; the scaffold store is imported only by the other three files in . The whole editor is unreachable dead code after Phase 2.                                                                                    | n/a - UI-only state (routing/wiring); the target data comes from atlas/app.py:589-591 (GET /api/workflows/{id})                                                                                                                                                                                                                                                                                                                                                                          | Phase 3 must mount the rewritten editor at src/routes/\_app/workflows.$id.tsx, which currently renders the Phase 2 read-only Atlas view. That route already sits under the authenticated \_app layout and must keep its loader + errorComponent + notFoundComponent (CLAUDE.md routing rules). Decide explicitly whether the read-only view stays as a view/edit toggle or is replaced.                                                                                                                                                                                                                                                                            |
| [Atlas-supported, never in this editor] Workflow policy fields                                                    | n/a - no policy UI exists anywhere in workflow-editor.tsx                                                    | The editor has no concept of workflow policy; save() sends only nodes and edges.                                                                                                                                                              | docs/specs/workflow-definition.schema.json ($defs/policy: max_jobs, max_iterations, max_attempts_per_node, max_minutes, requires_human_after_iterations, max_budget_units, allowed_worker_ids, allowed_workspace_ids, stop_on_first_failure, file_handoff); atlas/workflows.py:231-232 (a cycle without policy.max_iterations or a max_iterations_below guard is rejected); atlas/workflows.py:219-222 (edge push_files requires policy.file_handoff=true)                               | Add a workflow-settings panel in the chrome (header "Settings" or a policy tab) writing `policy` through the same PUT. It is not optional cosmetics: without policy.max_iterations the loop/back-edge story is unbuildable (validate_workflow_graph rejects the graph), and allowed_worker_ids/allowed_workspace_ids drive the worker picker's filtering.                                                                                                                                                                                                                                                                                                          |
| [Atlas-supported, never in this editor] Workflow rename, description, version and status                          | workflow-editor.tsx:481-483 (name is rendered read-only; save() at 371-394 never sends metadata)             | Name is display-only; description, version and status are not surfaced or writable at all.                                                                                                                                                    | atlas/db.py:1102-1110 (PUT persists name, description, version, status); atlas/app.py:1314-1330 (_validate_workflow_metadata: version must be an integer >= 1, status must match [A-Za-z0-9_-]+)                                                                                                                                                                                                                                                                                         | Add an inline rename on the <h1> plus a description field, both sent on PUT. Surface status as a badge (draft/active/etc., constrained to the [A-Za-z0-9_-]+ token Atlas enforces). `version` is the only handle for optimistic-concurrency-by-convention available - Phase 3 should read it on load and refuse/warn on save if the server copy moved, since Atlas offers no ETag.                                                                                                                                                                                                                                                                                 |
| [Atlas-supported, never in this editor] Local layout persistence                                                  | workflow-editor.tsx:379-380 (x/y are written into the store payload as if they were domain data)             | Node positions are treated as part of the saved workflow; there is no local layout store, no viewport persistence, and no auto-layout.                                                                                                        | docs/specs/workflow-visual-builder-spec-en.md:36,56 (layout state does not affect execution and must not change Semantic JSON); docs/specs/workflow-visual-builder-spec-en.md:600-624 (recommended layout envelope: layout_version, nodes{x,y}, viewport; auto-layout for unpositioned nodes); atlas/db.py:1102-1110 (no layout column - Atlas has nowhere to store it)                                                                                                                  | Build a localStorage layout store keyed by workflow id (+ graph version), holding {layout_version, nodes:{id:{x,y}}, viewport}, with a layered left-to-right auto-layout fallback and cleanup of entries for deleted nodes. Document clearly in the UI that layout is per-device and does not sync between users - that is a real regression from what the scaffold's Save appeared to promise, and Atlas cannot fix it.                                                                                                                                                                                                                                           |
| Node duplicate                                                                                                    | workflow-editor.tsx:343-359                                                                                  | Does not exist - no duplicate/clone handler, no node context menu, no copy/paste key handling anywhere.                                                                                                                                       | atlas/workflows.py:165-167 only requires node ids to be unique, so a copied node with a fresh id is fully valid Atlas                                                                                                                                                                                                                                                                                                                                                                    | Worth adding in Phase 3: duplicating a configured worker (prompt, routing, budget, outputs) is the single highest-value editor shortcut and Atlas has no objection. Must mint a new unique id, NOT copy the outputs artifact key blindly (two nodes writing the same key collide), drop incident edges, and offset the position in the local layout store.                                                                                                                                                                                                                                                                                                         |
| Edge condition types the editor cannot author                                                                     | workflow-editor.tsx:258-287 (no edge inspector exists)                                                       | The scaffold has no edge inspector at all - edges carry only a cosmetic label and no semantics.                                                                                                                                               | workflow-definition.schema.json $defs/condition supports always, artifact_equals {artifact, path?, value}, artifact_in {artifact, path?, values[]}, manager_selected {target}, human_selected {choice}, max_iterations_below {node, max}; enforced at atlas/workflows.py:223-239                                                                                                                                                                                                         | Build the edge inspector as new work. It is the single largest Atlas capability the editor never had, and without it conditions, loop guards and manager routing are all unauthorable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Workflow policy fields                                                                                            | n/a - absent from the scaffold entirely                                                                      | The scaffold has no policy concept anywhere - no budget, iteration, timeout, or failure-handling settings.                                                                                                                                    | atlas/workflows.py:259-266 WORKFLOW_POLICY_LIMITS and validate_workflow_policy: max_jobs<=100, max_iterations<=100, max_attempts_per_node<=25, max_minutes<=1440, requires_human_after_iterations<=100, max_budget_units<=1000000, stop_on_first_failure and file_handoff booleans, plus allowed_worker_ids/allowed_workspace_ids in the schema                                                                                                                                          | Add a workflow settings panel. policy is a REQUIRED top-level field in workflow-definition.schema.json, and policy.max_iterations is the only way to save a graph containing a cycle (atlas/workflows.py:255-256). Validate each field against the documented caps client-side so the user sees the bound rather than a server 400.                                                                                                                                                                                                                                                                                                                                |
| Trigger kinds the editor never had                                                                                | workflow-scaffold-store.ts:556 (a single generic trigger node)                                               | The scaffold models triggers as one node kind with a webhook/schedule/event mode toggle.                                                                                                                                                      | docs/specs/workflow-trigger.schema.json $defs/trigger: manual, webhook, schedule, workflow_run_completed, artifact_created, worker_status_changed - each with a closed config; atlas/workflows.py:59-73 mirrors the closed key sets; CRUD at atlas/app.py:759-795 including POST /api/workflow-triggers/{id}/fire                                                                                                                                                                        | Build a trigger panel as a separate resource view: create/enable/disable/delete/fire, per-type config forms (schedule's interval_minutes XOR daily_time; artifact_created's kind enum text\|json\|markdown\|file_ref\|summary\|decision), and never place a trigger in graph.nodes. Note artifact_created and worker_status_changed have no scaffold ancestor at all.                                                                                                                                                                                                                                                                                              |
| Per-node execution, budget and file-collection fields                                                             | n/a - absent from DEFAULT_NODE_CONFIG (workflow-editor.tsx:57-84)                                            | The scaffold's worker/manager config carries only worker, workspace, prompt and output.                                                                                                                                                       | atlas/workflows.py:196-197 (budget_units positive int), atlas/workflows.py:199-202 (execution must be a JOB_EXECUTION_MODES value), atlas/workflows.py:203-212 (collect_files validated at save time against the artifact cap); schema also allows model, company, role, tags, output_format                                                                                                                                                                                             | Add these to the worker/manager inspector. execution:"callback" in particular changes run semantics (the runner waits on a callback deadline, atlas/workflows.py:38-51) and is invisible in the current UI. collect_files is validated at SAVE time, so its errors must land on the node.                                                                                                                                                                                                                                                                                                                                                                          |
| Edge condition authoring (the closed condition set)                                                               | n/a - never existed in the scaffold                                                                          | Atlas edges carry a required condition object from a closed set; the scaffold has no edge inspector at all and encodes branching in node kinds and free-text labels.                                                                          | docs/specs/workflow-definition.schema.json:142-201 and atlas/workflows.py:1920-1952 - allowed types are exactly always, artifact_equals {artifact, path?, value}, artifact_in {artifact, path?, values}, manager_selected {target}, human_selected {choice}, max_iterations_below {node, max}                                                                                                                                                                                            | Build an edge inspector that authors all six types, with per-type field forms and an artifact-key picker sourced from upstream worker outputs. Enforce the coupling rules client-side too: a manager source edge MUST be manager_selected with target == edge.to (atlas/workflows.py:224-229), and a human_gate with choices MUST use human_selected with a declared choice id (atlas/workflows.py:233-239). Unknown condition types coming back from Atlas fail closed in the UI.                                                                                                                                                                                 |
| Join quorum validation                                                                                            | n/a - never existed in the scaffold                                                                          | The scaffold reads config.quorum as a string with a '2' default and no validation; Atlas requires a positive integer bounded by the incoming edge count.                                                                                      | atlas/workflows.py:177-182 (positive integer required) and atlas/workflows.py:248-253 ('quorum exceeds distinct incoming upstream count')                                                                                                                                                                                                                                                                                                                                                | Add a live validation rule in the join inspector: integer input, min 1, max = distinct incoming edges, recomputed when edges change (deleting an incoming edge can invalidate a previously valid quorum). Surface the error on the node before save, mirroring Atlas's message.                                                                                                                                                                                                                                                                                                                                                                                    |
| Workflow policy fields                                                                                            | n/a - never existed in the scaffold                                                                          | Atlas requires a policy object on every definition; the scaffold has no policy concept whatsoever.                                                                                                                                            | docs/specs/workflow-definition.schema.json:7 (policy required) and :240-257 (max_jobs, max_iterations, max_attempts_per_node, max_minutes, requires_human_after_iterations, max_budget_units, allowed_worker_ids, allowed_workspace_ids, stop_on_first_failure, file_handoff)                                                                                                                                                                                                            | Add a workflow-settings panel covering every policy field with the schema's min/max bounds. Two are load-bearing for the run area: policy.max_iterations is one of only two ways to make a cyclic graph savable (atlas/workflows.py:254-256), and policy.requires_human_after_iterations injects an approval that has no graph node (atlas/workflows.py:949-960) - the run UI must be able to render an approval whose node_key is not a human_gate.                                                                                                                                                                                                               |
| Trigger management as a separate resource                                                                         | n/a - scaffold models triggers as a graph node kind                                                          | The scaffold treats 'trigger' as a canvas node kind that the simulator uses as an entry point; Atlas has no such node type and manages triggers as their own resource.                                                                        | atlas/workflows.py:173 rejects a 'trigger' node type; docs/specs/workflow-trigger.schema.json defines the five/six kinds (manual, webhook, schedule with interval_minutes\|daily_time, workflow_run_completed, artifact_created, worker_status_changed); CRUD at atlas/app.py:759-770 and manual fire at atlas/app.py:772-782                                                                                                                                                            | Delete the trigger node kind and build a trigger panel: list/create/update/enable-disable, per-kind config forms with the schema's closed key sets (atlas/workflows.py:69-76), and a 'Fire now' action - noting Atlas refuses manual firing of the three event-driven kinds (atlas/app.py:774-775). Trigger events are readable at GET /api/workflow-triggers/{id}/events (atlas/app.py:783-788).                                                                                                                                                                                                                                                                  |
| Run pause / resume                                                                                                | n/a - never existed in the scaffold                                                                          | The scaffold has no pause or resume; its only run controls are Run and Clear.                                                                                                                                                                 | atlas/app.py:716-727 - POST /api/workflow-runs/{id}/pause and /resume (resume accepts {retry_interrupted: true}); backed by atlas/workflows.py:465-472 and 475-512, which also handles the recovery_required state (atlas/workflows.py:599)                                                                                                                                                                                                                                              | Add Pause/Resume/Cancel to the run header, enabled per run.state. Resume needs the retry_interrupted opt-in when nodes are in the 'interrupted' state, and a recovery_required run needs an explicit operator-authorized retry affordance - both are states the scaffold's five-value RunState cannot express.                                                                                                                                                                                                                                                                                                                                                     |
| Live per-node job event streaming (SSE)                                                                           | n/a - scaffold logs are fabricated locally                                                                   | The scaffold's 'live' drawer is a timer-driven fake; real live output comes from per-job SSE that the editor never consumed.                                                                                                                  | atlas/app.py:948-978 (\_stream_job_events: GET /api/jobs/{job_id}/events?after=<seq>, text/event-stream, explicit close frame). Critically there is NO run-level stream - GET /api/workflow-runs/{id}/events is persisted JSON only (atlas/app.py:709-714), as documented in docs/BACKEND_INTEGRATION.md:139                                                                                                                                                                             | This is Phase 4 work, but Phase 3 must not design it out: the run view combines (a) polled GET /api/workflow-runs/{id} for run/node/approval state, (b) polled or paged GET .../events for workflow history, and (c) per-node SSE keyed by the runtime node's job_id, proxied same-origin so the bearer stays server-side. Dedupe by seq; treat EOF without a close frame as a disconnect; there is no heartbeat.                                                                                                                                                                                                                                                  |
| Run artifacts and artifact download                                                                               | n/a - never existed in the scaffold                                                                          | Atlas runs produce artifacts (worker outputs) that the scaffold has no concept of - its nodes just 'complete'.                                                                                                                                | atlas/app.py:676-682 (GET /api/workflow-runs/{id}/artifacts) and atlas/app.py:705-707 (GET /api/artifacts/{id}/content); worker nodes declare exactly one output artifact key (docs/specs/workflow-definition.schema.json:40-45)                                                                                                                                                                                                                                                         | Add an artifacts tab/panel to the run view and a per-node artifact affordance, with download proxied through a server function (no bearer in the browser). Artifact keys also feed the edge inspector's artifact_equals/artifact_in pickers, so this is a Phase 3 dependency, not only a display feature.                                                                                                                                                                                                                                                                                                                                                          |
| Pre-run validation against Atlas                                                                                  | n/a - the scaffold's Run has no validation at all                                                            | Run always fires regardless of graph validity; Atlas has an explicit validate endpoint the editor never called.                                                                                                                               | atlas/app.py:607-616 (POST /api/workflows/{id}/validate) plus repair/explain at atlas/app.py:618-631; validation also runs again at save and at run start (atlas/workflows.py:424)                                                                                                                                                                                                                                                                                                       | Wire validate into the editor: run it on the saved definition before enabling Run, and map each returned error to the offending node or edge in the inspector. Note the documented limitation (docs/specs/workflow-visual-builder-spec-en.md:790-793): validate requires a SAVED workflow id, so an unsaved draft needs equivalent client-side schema/semantic checks.                                                                                                                                                                                                                                                                                             |
| Edge inspector with the six Atlas condition types                                                                 | workflow-editor.tsx:706-1167 (NodeInspector has no edge mode)                                                | Does not exist: the scaffold can only select nodes, so no edge condition can be authored at all.                                                                                                                                              | atlas-control-plane/docs/specs/workflow-definition.schema.json:142-201 (condition oneOf: always, artifact_equals{artifact,path,value}, artifact_in{artifact,path,values[]}, manager_selected{target}, human_selected{choice}, max_iterations_below{node,max}); atlas/workflows.py:219-239 enforces the source-type rules                                                                                                                                                                 | Build an edge inspector - this is the single biggest gap. It needs a condition-type picker with per-type fields, upstream artifact-key suggestions from the source node's outputs[0], and the source-type constraints baked in (manager source forces manager_selected with target == edge.to; a gate with choices forces human_selected referencing a declared choice). Edge selection state must be added to the editor, which today only tracks `selected` node ids.                                                                                                                                                                                            |
| Workflow policy panel                                                                                             | workflow-editor.tsx:706-1167 (no policy UI anywhere)                                                         | Does not exist: the scaffold has no notion of workflow-level guards.                                                                                                                                                                          | atlas-control-plane/docs/specs/workflow-definition.schema.json:240-256 and atlas/workflows.py:262-288 (WORKFLOW_POLICY_LIMITS: max_jobs<=100, max_iterations<=100, max_attempts_per_node<=25, max_minutes<=1440, requires_human_after_iterations<=100, max_budget_units<=1000000, plus stop_on_first_failure and file_handoff booleans and the allowed_worker_ids/allowed_workspace_ids lists)                                                                                           | Add a policy inspector mode. `policy` is REQUIRED by the canonical definition schema (line 7), so the editor cannot save without it. Enforce each integer range client-side, expose stop_on_first_failure (runtime default is true, atlas/workflows.py:1107), the two allowlists (validated against live workers/workspaces), and file_handoff which gates edge push_files. max_iterations here is also what legalises a back-edge cycle.                                                                                                                                                                                                                          |
| Start node selection                                                                                              | workflow-editor.tsx:706-1167 (no 'Set as start'; the scaffold used a trigger node instead)                   | Does not exist: the scaffold expressed 'where does it begin' as a trigger node on the canvas.                                                                                                                                                 | atlas/workflows.py:209-213 (graph requires a `start` that references an existing node); docs/specs/workflow-visual-builder-spec-en.md:120-125 (exactly one start, visible Start badge, 'Set as start' action, start cannot be deleted unguarded)                                                                                                                                                                                                                                         | Add a Start badge on the canvas node and a 'Set as start' action in the node inspector/context menu, and block deleting the start node. This replaces the trigger node's role in the graph; without it no workflow can be saved.                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Node id field with atomic rename refactor                                                                         | workflow-editor.tsx:344-347 (ids are generated as n\_<random> and never editable)                            | Does not exist: node ids are opaque random strings the user never sees or controls.                                                                                                                                                           | atlas/workflows.py:165-171 (id must be a unique non-empty string; it is the node's identity in start/edges/conditions); docs/specs/workflow-visual-builder-spec-en.md:262-268 (recommended pattern and the atomic-rename requirement)                                                                                                                                                                                                                                                    | Expose the id as an editable field with the ^[A-Za-z\_][A-Za-z0-9_]\*$ pattern and uniqueness check, and implement the atomic rename across graph.start, edge from/to, manager_selected.target and max_iterations_below.node. Ids are user-facing in Atlas errors, run events and approvals, so random n_ab12c strings make every backend message unreadable.                                                                                                                                                                                                                                                                                                      |
| Worker/manager routing and execution fields (role, tags, model, company, workspace_key, execution, collect_files) | workflow-editor.tsx:787-868 (worker inspector exposes only worker/workspace/output/budget/structured_output) | Does not exist: the scaffold has no role/tag routing, no model override, no execution mode and no file collection.                                                                                                                            | atlas-control-plane/docs/specs/workflow-definition.schema.json:29-52 (worker_id, workspace_id, workspace_key, company, model, role, tags, execution enum stream\|callback, collect_files) and atlas/workflows.py:201-215 (execution enum + collect_files validated at save time), atlas/workflows.py:344-350 (a role with no matching worker is rejected)                                                                                                                                | Add these to the Advanced disclosure for both worker and manager (manager currently has NO advanced block at all). role/tags are what make the 'Auto' worker option meaningful. collect_files must be validated against artifact_max_files_cap() expectations client-side since Atlas rejects a bad list at save, not at run.                                                                                                                                                                                                                                                                                                                                      |
| Edge file handoff (push_files)                                                                                    | workflow-editor.tsx:706-1167 (no edge fields at all)                                                         | Does not exist: the scaffold has no concept of passing files between steps.                                                                                                                                                                   | atlas-control-plane/docs/specs/workflow-definition.schema.json:210-215 (edge.push_files, artifact-key glob patterns, requires policy.file_handoff) paired with schema:253-256 (policy.file_handoff)                                                                                                                                                                                                                                                                                      | Once the edge inspector exists, add push_files behind a policy.file_handoff check - the control should be visibly disabled with an explanatory hint until file_handoff is enabled in the policy panel, since Atlas treats it as opt-in and off by default.                                                                                                                                                                                                                                                                                                                                                                                                         |
| Trigger kinds artifact_created and worker_status_changed (plus name/enabled)                                      | workflow-editor.tsx:874-892 (only four modes, and no name or enabled control)                                | Does not exist: the scaffold's trigger node covers only webhook/schedule/manual/event and has no enable switch or trigger name.                                                                                                               | atlas/workflows.py:59 \_TRIGGER_STATES includes artifact_created and worker_status_changed; docs/specs/workflow-trigger.schema.json:72-105 (artifact_created config key/kind/source_workflow_definition_id; worker_status_changed config worker_id/status) and every trigger requires name + enabled (lines 12-17)                                                                                                                                                                       | The trigger panel must cover all six types, each with `name` and `enabled` (the plan's 'trigger enable/disable' work item). Note that the config objects for these two are CLOSED (atlas/workflows.py:65-74 \_TRIGGER_CONFIG_KEYS) - a misspelled filter key is rejected rather than ignored, so the UI must emit exactly the allowed keys and omit empty filters.                                                                                                                                                                                                                                                                                                 |
| Validation surface (Validate action + error panel)                                                                | workflow-editor.tsx:371-394 (save() never validates anything)                                                | Does not exist: the scaffold saves whatever is on the canvas into a mock store; nothing can fail.                                                                                                                                             | atlas/workflows.py:149-256 (validate_workflow_graph raises a specific ValueError naming the offending node/edge index) and atlas/workflows.py:304-350 (validate_workflow_references); docs/specs/workflow-visual-builder-spec-en.md:96-98 (the error panel must be discoverable without hover and selecting an error must focus the affected node/edge/field)                                                                                                                            | Add a Validate action plus an error panel that maps Atlas's node-id / edge-index messages back to canvas selection and inspector fields - this is the plan's exit criterion 'Atlas validation errors appear at the relevant node/edge/form field'. Run local validation before the PUT so the common failures (missing start, unguarded cycle, quorum too high, manager edge without manager_selected, gate choice without an edge) are caught before the round trip.                                                                                                                                                                                              |

---

## 6. Phase 3 acceptance checklist

Derived from the tables above. This is the list to run at the Phase 3 gate, so "complete" is
decided against something written down before the work started.

### Palette and graph model

- [x] Palette exposes exactly four kinds: `worker`, `manager`, `join`, `human_gate`.
- [x] `trigger`, `condition`, `decision`, `loop`, `fanout` are gone from the palette, the types, and the presentation map.
- [x] The internal kind is `human_gate`; "Approval" survives only as a display label, with no `approval` API alias and no round-trip mapping.
- [x] "Ask to choose" and "Approval" both emit `type: "human_gate"` — with and without `choices`.
- [x] A `manager` node always emits `schema: "manager_decision_v1"`.
- [x] A `join` node always emits `mode`; `quorum` is a positive integer not exceeding the distinct incoming-edge count.
- [x] `graph.start` is selectable on a node ("Set as start"), shown as a badge, and cannot be deleted until another start is selected.
- [x] An unknown node or condition type coming back from Atlas **fails closed** in the UI and is never sent back.

### Edges

- [x] An edge inspector exists and can author all six condition types: `always`, `artifact_equals`, `artifact_in`, `manager_selected`, `human_selected`, `max_iterations_below`.
- [x] Every saved edge carries a `condition`; the UI default is `{"type": "always"}`.
- [x] The edge label is a **render of the condition**, never a stored free-text string that can drift.
- [x] A `manager` source node's outgoing edges are forced to `manager_selected` with `target` equal to the edge's `to`.
- [x] A `human_gate` with `choices` requires `human_selected` outgoing edges.
- [x] Creating a cycle selects the new edge and prompts for its guard (`policy.max_iterations` or edge `max_iterations_below`); save remains blocked without one.

### Persistence and layout

- [x] Save calls Atlas (`POST`/`PUT /api/workflows`) and sends **semantic JSON only** — no `x`, `y`, viewport, colours, or unknown keys.
- [x] Node positions and viewport are stored **locally** (localStorage), keyed by workflow id + graph version, with an auto-layout fallback when absent.
- [x] Moving a node never changes the semantic JSON, and therefore never marks the workflow dirty on the server.
- [x] Workflow name and description are editable and saved. Status is displayed read-only: the Phase 3 client write contract intentionally sends only name, description, graph, and policy; version is read for the local lost-update guard, not edited.
- [x] A policy panel exists for the Atlas policy fields and enforces their limits.
- [x] Renaming a node id atomically updates `graph.start`, every edge `from`/`to`, every `manager_selected.target`, and every `max_iterations_below.node`.

### Validation

- [x] Validation runs before save, enable, and run.
- [x] Atlas validation errors are shown at the offending node, edge, or field — not as one banner.
- [x] Round-trip fixtures pass for all four native kinds (serialize → Atlas → parse back), as required by `IMPLEMENTATION_PLAN.md` before save/run is wired.

### Run

- [x] Run calls `POST /api/workflow-runs` and yields a real Atlas run id.
- [x] No `setTimeout`/`setInterval` drives node state anywhere.
- [x] Canvas highlighting comes from Atlas runtime node state, not from local timers.
- [x] Pause, resume, and cancel are wired; `recovery_required` shows its warning and requires explicit retry authorization.
- [x] Approval decisions (approve / reject / choose) call `/api/approvals/{id}/…`.
- [x] The persisted log view is bounded and incrementally rendered; live per-job streaming remains Phase 4.

### Triggers

- [x] Triggers are managed in a separate panel against `/api/workflow-triggers`, never inside `graph.nodes`.
- [x] All Atlas trigger types are offered: `manual`, `schedule`, `webhook`, `workflow_run_completed`, `artifact_created`, `worker_status_changed`.
- [x] A schedule trigger takes **either** `interval_minutes` **or** `daily_time` (`HH:MM`), never a free-text path or time.
- [x] Each trigger has a name and an enabled toggle.

### Not-regressions

- [x] No control in the editor is present but non-functional (the scaffold shipped a dead "Check" button).
- [x] Navigating away with unsaved changes warns (the scaffold silently discarded them).
- [x] Deleting a non-start node via the keyboard requires confirmation; after confirmation it marks the editor dirty and clears the inspector. Start-node deletion is blocked.

---

## 7. Provenance and how to regenerate

Method: five readers, one per editor surface, each extracting capabilities from the source with
`file:line`; each area's output then passed to a mapper that decided the verdict against Atlas's
own code and specs; then two critics — one re-reading the editor for anything the readers missed,
one enumerating Atlas's workflow model independently so the gap analysis had a "what Atlas can do"
side. 12 agents, ~1.04M tokens.

Sources read for the Atlas side:

- `atlas/workflows.py` (`validate_workflow_graph`, the run loop, trigger firing, recovery)
- `atlas/app.py` (the `/api/workflows`, `/api/workflow-runs`, `/api/workflow-triggers`, `/api/approvals` routes)
- `docs/specs/workflow-definition.schema.json`
- `docs/specs/workflow-trigger.schema.json`
- `docs/specs/workflow-visual-builder-spec-en.md`

**Reconciliation notes (2026-07-20).** The Phase 3 recommendations above are now implemented
where the Atlas contract permits. The two earlier document contradictions are settled:

1. Triggers belong to Phase 3; the remaining static domain pages belong to Phase 5.
2. The mock store was deleted, so the editor uses `human_gate` directly and has no legacy
   `approval` migration.

Per-job SSE remains an explicit Phase 4 item. It is not marked complete merely because Phase 3
polls/refetches persisted run state.
